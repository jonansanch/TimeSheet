# Handoff - 2026-05-14

## Resumen

La sesion quedo al cierre de **Story 4.2: Asignar y Modificar Roles**.

Estado por epic:

- Epic 2: cerrada y QA aprobada.
- Epic 3: implementada completa; Jonathan confirmo que quedo funcional antes de iniciar Epic 4. Las stories 3.1 a 3.6 siguen documentalmente en `review`.
- Epic 4: en progreso.
  - Story 4.1 implementada y validada funcionalmente por Jonathan.
  - Story 4.2 implementada y pendiente de QA funcional en navegador.

## Punto exacto para continuar manana

1. Arrancar backend y frontend.
2. Entrar como Admin.
3. Probar Story 4.2 en `/admin/usuarios`.
4. Si QA 4.2 queda bien, continuar con **Story 4.3: Gestionar Empleados**.

Story actual:

- `Docs/stories/4.2-asignar-y-modificar-roles.md`
- `Status: review`

## Ultima validacion tecnica

Ejecutado despues de implementar 4.2:

```powershell
dotnet test Backend\KPG.Timesheet.sln --no-restore
dotnet build Backend\KPG.Timesheet.sln --no-restore
dotnet build Fronted\KPG.Timesheet.WebUI.sln --no-restore
```

Resultado:

- Backend tests: 88/88 pasan.
- Backend build: 0 errores, 0 warnings.
- Frontend build: 0 errores, 0 warnings.

## Usuarios de prueba

| Email | Password | Rol esperado |
|---|---|---|
| `admin@kpg.com` | `Admin1234!` | Admin |
| `supervisor@kpg.com` | `Supervisor1234!` | Supervisor |
| `empleado@kpg.com` | `Empleado1234!` | Empleado |
| `ana.garcia@kpg.com` | `Empleado1234!` | Empleado |
| `carlos.ruiz@kpg.com` | `Empleado1234!` | Empleado |

## Como arrancar

Verificar puertos:

```powershell
Get-NetTCPConnection -LocalPort 5100,5101,5200,5201 -ErrorAction SilentlyContinue |
    Select-Object LocalAddress,LocalPort,State,OwningProcess |
    Sort-Object LocalPort
```

Backend:

```powershell
dotnet run --project Backend\src\Api\KPG.Timesheet.Api.csproj --launch-profile https
```

Frontend:

```powershell
dotnet run --project Fronted\src\WebUI\KPG.Timesheet.WebUI.csproj --launch-profile https
```

Abrir:

```text
https://localhost:5201
```

## QA recomendado para Story 4.2

Pantalla:

- `/admin/usuarios`

Pasos:

- Login como `admin@kpg.com`.
- Cambiar rol de `empleado@kpg.com` a `Supervisor`.
- Confirmar que la tabla actualiza la fila con el nuevo rol.
- Cerrar sesion e iniciar como `empleado@kpg.com`; verificar que el rol nuevo se refleja despues de relogin/refresh.
- Volver a Admin y cambiar el rol de regreso a `Empleado` si se quiere dejar datos base limpios.
- Intentar cambiar el rol del propio `admin@kpg.com`; debe rechazarse.
- Intentar quitar el rol `Admin` al ultimo admin activo; debe rechazarse con mensaje claro.
- Cambiar rol a un usuario inactivo; debe actualizar rol sin activar la cuenta.

## Implementado en Story 4.1

Backend:

- `ApplicationUser` extendido con `IsActive`, `Created`, `DeactivatedAt`, `DeactivatedBy`.
- `IIdentityService` e `IdentityService` con listado, creacion, activacion, desactivacion y eliminacion segura.
- CQRS bajo `Backend/src/Application/Features/Users/`.
- Endpoints Admin bajo `/api/users`.
- Login/refresh bloquean usuarios inactivos.
- Tests de validator e Identity admin.

Frontend:

- `/admin/usuarios`.
- `UserAdminRepository`.
- `KpgUserDialog`.
- Navegacion Admin desde Home/NavMenu.
- Ajustes posteriores:
  - errores de Identity/Validation se muestran claros;
  - modales en tamano medio (`MaxWidth.Small`, `FullWidth=true`);
  - formulario de alta sin autocompletado en email/password.

## Implementado en Story 4.2

Backend:

- `ChangeUserRoleCommand`, handler y validator.
- `IIdentityService.ChangeUserRoleAsync`.
- `IdentityService.ChangeUserRoleAsync`:
  - reemplaza rol unico;
  - valida roles permitidos;
  - no activa usuarios inactivos;
  - bloquea dejar el sistema sin Admin activo.
- Endpoint `PUT /api/users/{id}/role`.
- Tests:
  - validator;
  - cambio de rol;
  - rol invalido no muta;
  - usuario inexistente;
  - usuario inactivo conserva estado;
  - ultimo Admin activo protegido;
  - otro Admin activo permite cambio;
  - credentials/refresh obtienen roles actualizados.

Frontend:

- `ChangeUserRoleRequest`.
- `IUserAdminRepository.ChangeRoleAsync`.
- `UserAdminRepository.ChangeRoleAsync`.
- `KpgUserRoleDialog.razor`.
- Boton de cambiar rol en `UsuariosAdminPage.razor`.

## Archivos principales tocados en Story 4.2

Backend:

- `Backend/src/Application/Common/Interfaces/IIdentityService.cs`
- `Backend/src/Application/Features/Users/Commands/ChangeUserRole/ChangeUserRoleCommand.cs`
- `Backend/src/Application/Features/Users/Commands/ChangeUserRole/ChangeUserRoleCommandHandler.cs`
- `Backend/src/Application/Features/Users/Commands/ChangeUserRole/ChangeUserRoleCommandValidator.cs`
- `Backend/src/Infrastructure/Identity/IdentityService.cs`
- `Backend/src/Api/Endpoints/Users.cs`
- `Backend/tests/Application.UnitTests/Features/Users/ChangeUserRoleCommandValidatorTests.cs`
- `Backend/tests/Infrastructure.IntegrationTests/Users/IdentityServiceUserAdminTests.cs`

Frontend:

- `Fronted/src/WebUI/Infrastructure/Repositories/Models/UserAdminModels.cs`
- `Fronted/src/WebUI/Infrastructure/Repositories/IUserAdminRepository.cs`
- `Fronted/src/WebUI/Infrastructure/Repositories/UserAdminRepository.cs`
- `Fronted/src/WebUI/Features/Admin/Components/KpgUserRoleDialog.razor`
- `Fronted/src/WebUI/Features/Admin/Pages/UsuariosAdminPage.razor`

Docs:

- `Docs/stories/4.2-asignar-y-modificar-roles.md`
- `Docs/current-status.md`
- `Docs/handoff/handoff-2026-05-14.md`
- `Docs/session-logs/2026-05-14-epic-4-story-4-2-handoff.md`

## Proximo paso recomendado

QA funcional de Story 4.2. Si queda bien:

1. Marcar Story 4.2 como aceptada/cerrada.
2. Crear Story 4.3: Gestionar Empleados.
3. Implementar 4.3.

## Advertencias de continuidad

- No hay `_bmad-output/implementation-artifacts/sprint-status.yaml`; no buscar estado ahi.
- El repo esta muy sucio por trabajo acumulado y artefactos `bin/obj/.vs`. No revertir cambios no relacionados.
- Story 4.2 no requiere revocar access tokens vigentes: el nuevo rol se aplica en refresh/relogin, segun politica actual.
