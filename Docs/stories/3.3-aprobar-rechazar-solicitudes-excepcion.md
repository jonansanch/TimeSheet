# Story 3.3: Aprobar o Rechazar Solicitudes de Excepción

Status: review

## Story

As an admin,
I want revisar solicitudes de registro fuera de ventana,
So that pueda autorizar o rechazar casos excepcionales con trazabilidad.

## Acceptance Criteria

1. Given solicitudes de excepción pendientes, when el Admin abre la vista `/solicitudes-excepcion`, then ve una tabla con solicitante (email), fecha solicitada, antigüedad (días desde la solicitud), justificación y estado (Pendiente / Aprobada / Rechazada).

2. Given una solicitud pendiente, when el Admin hace clic en Aprobar y confirma, then la solicitud cambia a estado Aprobada, and el usuario solicitante puede registrar horas para esa fecha autorizada (el backend omite la restricción de ventana para esa combinación userId+fecha con excepción aprobada).

3. Given una solicitud pendiente, when el Admin hace clic en Rechazar y confirma, then la solicitud cambia a estado Rechazada, and el usuario no puede registrar para esa fecha sin una nueva solicitud.

4. Given una solicitud que ya fue aprobada o rechazada, when el Admin la visualiza, then los botones Aprobar/Rechazar están deshabilitados (solo se puede actuar sobre Pendiente).

## Tasks / Subtasks

### Backend — Dominio

- [x] Agregar métodos de transición de estado en SolicitudExcepcion (AC: 2, 3, 4)
  - [x] En `Backend/src/Domain/Entities/SolicitudExcepcion.cs`, agregar `public void Aprobar()` que lanza `InvalidOperationException` si estado no es Pendiente, y asigna `Estado = EstadoSolicitud.Aprobada`.
  - [x] Agregar `public void Rechazar()` con la misma guarda y asigna `Estado = EstadoSolicitud.Rechazada`.

### Backend — Query GetSolicitudesExcepcion

- [x] Crear DTO para vista Admin (AC: 1)
  - [ ] Crear `Backend/src/Application/Features/SolicitudesExcepcion/Queries/GetSolicitudesExcepcion/SolicitudExcepcionAdminDto.cs` como `record(int Id, string UserId, string SolicitanteEmail, DateOnly FechaRegistro, string Justificacion, string Estado, DateTimeOffset Created)`.

- [x] Crear query GetSolicitudesExcepcion (AC: 1)
  - [x] Crear `Backend/src/Application/Features/SolicitudesExcepcion/Queries/GetSolicitudesExcepcion/GetSolicitudesExcepcionQuery.cs` como `record : IRequest<IEnumerable<SolicitudExcepcionAdminDto>>` con decorador `[Authorize(Roles = Roles.Admin)]`.
  - [x] Crear `GetSolicitudesExcepcionQueryHandler.cs` que:
    - Usa `IApplicationDbContext` + `IIdentityService`.
    - Lee todas las solicitudes ordenadas por Created DESC.
    - Por cada solicitud, resuelve email vía `IIdentityService.GetUserNameAsync(userId)` (email = UserName en este sistema).
    - Retorna la lista de `SolicitudExcepcionAdminDto`.

### Backend — Command AprobarSolicitudExcepcion

- [x] Crear command Aprobar (AC: 2)
  - [x] Crear `Backend/src/Application/Features/SolicitudesExcepcion/Commands/AprobarSolicitudExcepcion/AprobarSolicitudExcepcionCommand.cs` como `record(int Id) : IRequest` con `[Authorize(Roles = Roles.Admin)]`.
  - [x] Crear `AprobarSolicitudExcepcionCommandHandler.cs` que:
    - Lee la solicitud por Id. Si no existe, lanza `NotFoundException`.
    - Llama `solicitud.Aprobar()`.
    - Guarda cambios.

### Backend — Command RechazarSolicitudExcepcion

- [x] Crear command Rechazar (AC: 3)
  - [x] Crear `Backend/src/Application/Features/SolicitudesExcepcion/Commands/RechazarSolicitudExcepcion/RechazarSolicitudExcepcionCommand.cs` como `record(int Id) : IRequest` con `[Authorize(Roles = Roles.Admin)]`.
  - [x] Crear `RechazarSolicitudExcepcionCommandHandler.cs` que:
    - Lee la solicitud por Id. Si no existe, lanza `NotFoundException`.
    - Llama `solicitud.Rechazar()`.
    - Guarda cambios.

### Backend — Permitir registro con excepción aprobada

- [x] Modificar CreateRegistroHorasCommandHandler para respetar excepción aprobada (AC: 2)
  - [x] En `Backend/src/Application/Features/RegistroHoras/Commands/CreateRegistroHoras/CreateRegistroHorasCommandHandler.cs`, ANTES de lanzar la `ValidationException` por fecha fuera de ventana, verificar si existe una `SolicitudExcepcion` con `UserId == userId && FechaRegistro == request.FechaRegistro && Estado == EstadoSolicitud.Aprobada`.
  - [x] Si existe tal excepción aprobada, omitir el error de ventana y continuar con el registro (marcar `esRetroactivo = true`).

### Backend — Endpoints

- [x] Agregar endpoints Admin en SolicitudesExcepcion.cs (AC: 1, 2, 3)
  - [x] En `Backend/src/Api/Endpoints/SolicitudesExcepcion.cs`, agregar dentro de `Map()`:
    - `groupBuilder.MapGet("/", GetAll).RequireAuthorization()` → retorna 200 con lista.
    - `groupBuilder.MapPost("/{id:int}/aprobar", Aprobar).RequireAuthorization()` → retorna 204.
    - `groupBuilder.MapPost("/{id:int}/rechazar", Rechazar).RequireAuthorization()` → retorna 204.

### Backend — Tests

- [x] Tests del dominio: Aprobar/Rechazar (AC: 2, 3, 4)
  - [x] En `Backend/tests/Domain.UnitTests/` (o crear carpeta `SolicitudesExcepcion`), agregar tests:
    - `Aprobar_WhenPendiente_CambiaEstado()` → Estado queda Aprobada.
    - `Rechazar_WhenPendiente_CambiaEstado()` → Estado queda Rechazada.
    - `Aprobar_WhenNoEsPendiente_ThrowsInvalidOperation()` → ya aprobada no puede re-aprobarse.
    - `Rechazar_WhenNoEsPendiente_ThrowsInvalidOperation()` → ya rechazada no puede rechazarse.

- [x] Tests de integración: Handlers Aprobar/Rechazar (AC: 2, 3)
  - [x] Crear `Backend/tests/Infrastructure.IntegrationTests/SolicitudesExcepcion/AprobarRechazarSolicitudExcepcionTests.cs` con:
    - Aprobar solicitud Pendiente → estado Aprobada en DB.
    - Rechazar solicitud Pendiente → estado Rechazada en DB.
    - Aprobar solicitud inexistente → NotFoundException.
    - Aprobar solicitud ya aprobada → InvalidOperationException (propagado desde dominio).

- [x] Test de integración: Registro permitido con excepción aprobada (AC: 2)
  - [x] En `Backend/tests/Infrastructure.IntegrationTests/RegistroHoras/`, agregar test:
    - `Handle_WhenFechaFueraVentanaConExcepcionAprobada_ShouldCreateRegistro()`.

### Frontend — Repositorio Admin

- [x] Crear modelos y repositorio para Admin (AC: 1, 2, 3)
  - [x] Crear `Fronted/src/WebUI/Infrastructure/Repositories/Models/SolicitudExcepcionAdminModels.cs` con:
    - `record SolicitudExcepcionAdminResponse(int Id, string UserId, string SolicitanteEmail, DateOnly FechaRegistro, string Justificacion, string Estado, DateTimeOffset Created)`.
  - [x] Crear `Fronted/src/WebUI/Infrastructure/Repositories/ISolicitudExcepcionAdminRepository.cs` con:
    - `Task<List<SolicitudExcepcionAdminResponse>> GetAllAsync(CancellationToken ct = default)`.
    - `Task<bool> AprobarAsync(int id, CancellationToken ct = default)`.
    - `Task<bool> RechazarAsync(int id, CancellationToken ct = default)`.
  - [x] Crear `Fronted/src/WebUI/Infrastructure/Repositories/SolicitudExcepcionAdminRepository.cs` con patrón Bearer token idéntico a `SolicitudExcepcionRepository`.
  - [x] Registrar en `Program.cs`: `builder.Services.AddScoped<ISolicitudExcepcionAdminRepository, SolicitudExcepcionAdminRepository>()`.

### Frontend — Página Admin

- [x] Crear página SolicitudesExcepcionAdminPage (AC: 1, 2, 3, 4)
  - [ ] Crear `Fronted/src/WebUI/Features/Admin/Pages/SolicitudesExcepcionAdminPage.razor` con:
    - `@page "/solicitudes-excepcion"`.
    - `@attribute [Authorize(Roles = Roles.Admin)]`.
    - Título "Solicitudes de excepción" + subtítulo.
    - Indicador de carga (`MudProgressLinear Indeterminate`) mientras carga.
    - Alerta de error si falla la carga.
    - Alerta de info si no hay solicitudes.
    - `MudDataGrid<SolicitudExcepcionAdminResponse>` con columnas: Solicitante (email), Fecha, Antigüedad (días), Justificación, Estado (chip de color), Acciones.
    - Columna Estado: `MudChip` Color.Warning para Pendiente, Color.Success para Aprobada, Color.Error para Rechazada.
    - Columna Acciones: botón Aprobar (Color.Success, icono CheckCircle) y Rechazar (Color.Error, icono Cancel), ambos deshabilitados si estado ≠ "Pendiente" o si `_isProcesando`.
    - Al hacer clic Aprobar/Rechazar: mostrar `MudMessageBox` de confirmación. Si confirma, llamar al repositorio y recargar la fila (actualizar estado en lista local sin recargar toda la tabla).
    - Mostrar mensaje de éxito inline tras cada acción.

- [x] Agregar nav link Admin en NavMenu (AC: 1)
  - [x] En `Fronted/src/WebUI/Layout/NavMenu.razor`, dentro del bloque `<AuthorizeView Roles="@Roles.Admin">`, agregar `MudNavLink` con Href="/solicitudes-excepcion", Icon=`Icons.Material.Filled.PendingActions`, texto "Solicitudes".

- [x] Verificar compilación frontend (AC: 1, 2, 3)
  - [x] `dotnet build Fronted/KPG.Timesheet.WebUI.sln` → 0 errores, 0 advertencias.

## Dev Notes

### Contexto del proyecto
- Stack: .NET 10 + Blazor WASM + Clean Architecture + CQRS (MediatR) + SQL Server
- UI: MudBlazor 8.5.0. Tema: Primary=#0D3B5E (navy), Secondary=#5BB8D4 (sky), Warning=#FFD300, Error=#C62828
- Autenticación: JWT Bearer. RBAC via `[Authorize(Roles = "...")]` en commands/queries + `RequireAuthorization()` en endpoints
- `[Authorize]` = `KPG.Timesheet.Application.Common.Security.AuthorizeAttribute` (NO el de ASP.NET Core)
- Tests: xUnit + FluentAssertions + EF InMemory
- Roles constantes en `Domain/Constants/Roles.cs`: `Admin`, `Gerente`, `Supervisor`, `Empleado`

### Patrones críticos

**Modificación del dominio — estado inmutable con métodos:**
```csharp
// SolicitudExcepcion.cs — agregar estos dos métodos:
public void Aprobar()
{
    if (Estado != EstadoSolicitud.Pendiente)
        throw new InvalidOperationException("Solo se pueden aprobar solicitudes pendientes.");
    Estado = EstadoSolicitud.Aprobada;
}

public void Rechazar()
{
    if (Estado != EstadoSolicitud.Pendiente)
        throw new InvalidOperationException("Solo se pueden rechazar solicitudes pendientes.");
    Estado = EstadoSolicitud.Rechazada;
}
```

**Command con autorización de rol (mismo patrón que otros commands protegidos):**
```csharp
using KPG.Timesheet.Application.Common.Security;
using KPG.Timesheet.Domain.Constants;

[Authorize(Roles = Roles.Admin)]
public record AprobarSolicitudExcepcionCommand(int Id) : IRequest;
```

**Handler Aprobar/Rechazar:**
```csharp
public class AprobarSolicitudExcepcionCommandHandler : IRequestHandler<AprobarSolicitudExcepcionCommand>
{
    private readonly IApplicationDbContext _context;

    public AprobarSolicitudExcepcionCommandHandler(IApplicationDbContext context)
        => _context = context;

    public async Task Handle(AprobarSolicitudExcepcionCommand request, CancellationToken cancellationToken)
    {
        var solicitud = await _context.SolicitudesExcepcion
            .FindAsync([request.Id], cancellationToken);

        if (solicitud is null)
            throw new NotFoundException(nameof(SolicitudExcepcion), request.Id);

        solicitud.Aprobar();
        await _context.SaveChangesAsync(cancellationToken);
    }
}
```

**Query GetSolicitudesExcepcion (resolución de email via IIdentityService):**
```csharp
[Authorize(Roles = Roles.Admin)]
public record GetSolicitudesExcepcionQuery : IRequest<IEnumerable<SolicitudExcepcionAdminDto>>;

public class GetSolicitudesExcepcionQueryHandler
    : IRequestHandler<GetSolicitudesExcepcionQuery, IEnumerable<SolicitudExcepcionAdminDto>>
{
    private readonly IApplicationDbContext _context;
    private readonly IIdentityService _identityService;

    public GetSolicitudesExcepcionQueryHandler(IApplicationDbContext context, IIdentityService identityService)
    {
        _context = context;
        _identityService = identityService;
    }

    public async Task<IEnumerable<SolicitudExcepcionAdminDto>> Handle(
        GetSolicitudesExcepcionQuery request,
        CancellationToken cancellationToken)
    {
        var solicitudes = await _context.SolicitudesExcepcion
            .OrderByDescending(s => s.Created)
            .ToListAsync(cancellationToken);

        var result = new List<SolicitudExcepcionAdminDto>(solicitudes.Count);
        foreach (var s in solicitudes)
        {
            var email = await _identityService.GetUserNameAsync(s.UserId) ?? s.UserId;
            result.Add(new SolicitudExcepcionAdminDto(
                s.Id, s.UserId, email,
                s.FechaRegistro, s.Justificacion,
                s.Estado.ToString(), s.Created));
        }
        return result;
    }
}
```

**NOTA:** `GetUserNameAsync` devuelve `UserName` que en este sistema = Email (ver `IdentityService.CreateUserAsync` y seed: `UserName = "admin@kpg.com"`). Por tanto `email = await _identityService.GetUserNameAsync(s.UserId) ?? s.UserId` es correcto.

**NotFoundException — clase ya existe:**
```csharp
// Backend/src/Application/Common/Exceptions/NotFoundException.cs — ya existe en el proyecto
// Usar: throw new NotFoundException(nameof(SolicitudExcepcion), request.Id);
```

**Endpoints (ampliar el SolicitudesExcepcion.cs existente):**
```csharp
public class SolicitudesExcepcion : IEndpointGroup
{
    public static string RoutePrefix => "/api/solicitudes-excepcion";

    public static void Map(RouteGroupBuilder groupBuilder)
    {
        groupBuilder.MapPost("/", Create).RequireAuthorization();
        groupBuilder.MapGet("/", GetAll).RequireAuthorization();
        groupBuilder.MapPost("/{id:int}/aprobar", Aprobar).RequireAuthorization();
        groupBuilder.MapPost("/{id:int}/rechazar", Rechazar).RequireAuthorization();
    }

    [EndpointSummary("Listar todas las solicitudes de excepción (Admin)")]
    private static async Task<IResult> GetAll(ISender sender, CancellationToken ct)
    {
        var result = await sender.Send(new GetSolicitudesExcepcionQuery(), ct);
        return Results.Ok(result);
    }

    [EndpointSummary("Aprobar solicitud de excepción (Admin)")]
    private static async Task<IResult> Aprobar(int id, ISender sender, CancellationToken ct)
    {
        await sender.Send(new AprobarSolicitudExcepcionCommand(id), ct);
        return Results.NoContent();
    }

    [EndpointSummary("Rechazar solicitud de excepción (Admin)")]
    private static async Task<IResult> Rechazar(int id, ISender sender, CancellationToken ct)
    {
        await sender.Send(new RechazarSolicitudExcepcionCommand(id), ct);
        return Results.NoContent();
    }
    
    // ... Create ya existente
}
```

**Modificación en CreateRegistroHorasCommandHandler — verificar excepción aprobada:**
```csharp
// Reemplazar el bloque de validación de ventana existente:
if (request.FechaRegistro < earliestAllowed)
{
    // Verificar si existe excepción aprobada para este usuario y fecha
    var tieneExcepcionAprobada = await _context.SolicitudesExcepcion
        .AnyAsync(s =>
            s.UserId == userId &&
            s.FechaRegistro == request.FechaRegistro &&
            s.Estado == EstadoSolicitud.Aprobada,
            cancellationToken);

    if (!tieneExcepcionAprobada)
    {
        throw new ApplicationValidationException([
            new ValidationFailure(
                nameof(request.FechaRegistro),
                $"La fecha seleccionada está fuera de la ventana de registro permitida ({windowDays} días hábiles).")
        ]);
    }
    // Si tiene excepción aprobada, continuar → el registro se marcará esRetroactivo = true
}
```

**IMPORTANTE:** Agregar el using de `EstadoSolicitud`:
```csharp
using KPG.Timesheet.Domain.Enums;
```

### DTOs necesarios

```csharp
// SolicitudExcepcionAdminDto.cs
public record SolicitudExcepcionAdminDto(
    int Id,
    string UserId,
    string SolicitanteEmail,
    DateOnly FechaRegistro,
    string Justificacion,
    string Estado,
    DateTimeOffset Created);
```

### Tests de dominio — patrón

```csharp
public class SolicitudExcepcionEstadoTests
{
    [Fact]
    public void Aprobar_WhenPendiente_CambiaEstadoAAprobada()
    {
        var s = new SolicitudExcepcion("user-1", new DateOnly(2026, 4, 1), "Justificación");
        s.Aprobar();
        s.Estado.Should().Be(EstadoSolicitud.Aprobada);
    }

    [Fact]
    public void Rechazar_WhenPendiente_CambiaEstadoARechazada()
    {
        var s = new SolicitudExcepcion("user-1", new DateOnly(2026, 4, 1), "Justificación");
        s.Rechazar();
        s.Estado.Should().Be(EstadoSolicitud.Rechazada);
    }

    [Fact]
    public void Aprobar_WhenYaAprobada_ThrowsInvalidOperationException()
    {
        var s = new SolicitudExcepcion("user-1", new DateOnly(2026, 4, 1), "Justificación");
        s.Aprobar();
        var act = () => s.Aprobar();
        act.Should().Throw<InvalidOperationException>();
    }

    [Fact]
    public void Rechazar_WhenYaRechazada_ThrowsInvalidOperationException()
    {
        var s = new SolicitudExcepcion("user-1", new DateOnly(2026, 4, 1), "Justificación");
        s.Rechazar();
        var act = () => s.Rechazar();
        act.Should().Throw<InvalidOperationException>();
    }
}
```

### Frontend — Repositorio Admin (patrón idéntico al SolicitudExcepcionRepository existente)

```csharp
public class SolicitudExcepcionAdminRepository : ISolicitudExcepcionAdminRepository
{
    private readonly HttpClient _http;
    private readonly AuthStateService _authState;

    public SolicitudExcepcionAdminRepository(HttpClient http, AuthStateService authState)
    {
        _http = http;
        _authState = authState;
    }

    public async Task<List<SolicitudExcepcionAdminResponse>> GetAllAsync(CancellationToken ct = default)
    {
        if (string.IsNullOrWhiteSpace(_authState.AccessToken))
            return [];

        using var message = new HttpRequestMessage(HttpMethod.Get, "api/solicitudes-excepcion");
        message.Headers.Authorization = new AuthenticationHeaderValue("Bearer", _authState.AccessToken);

        var response = await _http.SendAsync(message, ct);
        if (!response.IsSuccessStatusCode)
            return [];

        return await response.Content.ReadFromJsonAsync<List<SolicitudExcepcionAdminResponse>>(
            cancellationToken: ct) ?? [];
    }

    public async Task<bool> AprobarAsync(int id, CancellationToken ct = default)
    {
        if (string.IsNullOrWhiteSpace(_authState.AccessToken))
            return false;

        using var message = new HttpRequestMessage(HttpMethod.Post, $"api/solicitudes-excepcion/{id}/aprobar");
        message.Headers.Authorization = new AuthenticationHeaderValue("Bearer", _authState.AccessToken);

        var response = await _http.SendAsync(message, ct);
        return response.IsSuccessStatusCode;
    }

    public async Task<bool> RechazarAsync(int id, CancellationToken ct = default)
    {
        if (string.IsNullOrWhiteSpace(_authState.AccessToken))
            return false;

        using var message = new HttpRequestMessage(HttpMethod.Post, $"api/solicitudes-excepcion/{id}/rechazar");
        message.Headers.Authorization = new AuthenticationHeaderValue("Bearer", _authState.AccessToken);

        var response = await _http.SendAsync(message, ct);
        return response.IsSuccessStatusCode;
    }
}
```

### Frontend — Estructura de la página Admin

La página debe:
1. Cargar en `OnInitializedAsync` → llamar `GetAllAsync()`
2. Mostrar tabla con columnas: Solicitante, Fecha, Antigüedad, Justificación, Estado (chip), Acciones
3. **Antigüedad** = `(DateTimeOffset.UtcNow - item.Created).Days` → mostrar "hace {n} día(s)"
4. Confirmar antes de Aprobar/Rechazar con `MudMessageBox`
5. Actualizar el estado en la lista local tras éxito (no recargar todo)
6. Estado chips: Warning=Pendiente, Success=Aprobada, Error=Rechazada

```razor
@page "/solicitudes-excepcion"
@attribute [Authorize(Roles = Roles.Admin)]
@inject ISolicitudExcepcionAdminRepository Repo
@inject IDialogService DialogService

<PageTitle>Solicitudes de excepción - KPG Timesheet</PageTitle>

<MudStack Spacing="3">
    <div>
        <MudText Typo="Typo.h4" Style="font-family:Poppins,sans-serif;font-weight:600;">
            Solicitudes de excepción
        </MudText>
        <MudText Typo="Typo.body2" Color="Color.Secondary">
            Autoriza o rechaza solicitudes de registro fuera de la ventana permitida.
        </MudText>
    </div>

    @if (_isLoading) { <MudProgressLinear Indeterminate="true" Size="Size.Small" /> }
    else if (!string.IsNullOrWhiteSpace(_errorMessage))
    {
        <MudAlert Severity="Severity.Error" Dense="true">@_errorMessage</MudAlert>
    }
    else if (_solicitudes.Count == 0)
    {
        <MudAlert Severity="Severity.Info" Icon="@Icons.Material.Filled.CheckCircle">
            No hay solicitudes pendientes.
        </MudAlert>
    }
    else
    {
        @if (!string.IsNullOrWhiteSpace(_successMessage))
        {
            <MudAlert Severity="Severity.Success" Dense="true">@_successMessage</MudAlert>
        }
        <MudDataGrid T="SolicitudExcepcionAdminResponse"
                     Items="_solicitudes"
                     Dense="true"
                     Filterable="false"
                     SortMode="SortMode.Single">
            <Columns>
                <PropertyColumn Property="x => x.SolicitanteEmail" Title="Solicitante" />
                <PropertyColumn Property="x => x.FechaRegistro" Title="Fecha solicitada">
                    <CellTemplate>@context.Item.FechaRegistro.ToString("dd/MM/yyyy")</CellTemplate>
                </PropertyColumn>
                <TemplateColumn Title="Antigüedad" Sortable="false">
                    <CellTemplate>
                        @{ var dias = (int)(DateTimeOffset.UtcNow - context.Item.Created).TotalDays; }
                        hace @dias día@(dias == 1 ? "" : "s")
                    </CellTemplate>
                </TemplateColumn>
                <PropertyColumn Property="x => x.Justificacion" Title="Justificación" Sortable="false" />
                <TemplateColumn Title="Estado" Sortable="false">
                    <CellTemplate>
                        <MudChip T="string"
                                 Size="Size.Small"
                                 Color="@GetEstadoColor(context.Item.Estado)">
                            @context.Item.Estado
                        </MudChip>
                    </CellTemplate>
                </TemplateColumn>
                <TemplateColumn Title="Acciones" Sortable="false">
                    <CellTemplate>
                        <MudIconButton Icon="@Icons.Material.Filled.CheckCircle"
                                       Color="Color.Success"
                                       Size="Size.Small"
                                       aria-label="Aprobar"
                                       Disabled="@(context.Item.Estado != "Pendiente" || _isProcesando)"
                                       OnClick="@(() => ConfirmarAprobarAsync(context.Item))" />
                        <MudIconButton Icon="@Icons.Material.Filled.Cancel"
                                       Color="Color.Error"
                                       Size="Size.Small"
                                       aria-label="Rechazar"
                                       Disabled="@(context.Item.Estado != "Pendiente" || _isProcesando)"
                                       OnClick="@(() => ConfirmarRechazarAsync(context.Item))" />
                    </CellTemplate>
                </TemplateColumn>
            </Columns>
        </MudDataGrid>
    }
    <MudMessageBox @ref="_msgBox" CancelText="Cancelar">
        <MessageContent>@_msgBoxContent</MessageContent>
        <YesButton>
            <MudButton Color="@_msgBoxColor" Variant="Variant.Filled">Confirmar</MudButton>
        </YesButton>
    </MudMessageBox>
</MudStack>
```

### NavMenu — agregar link (solo Admin)

Dentro del `<AuthorizeView Roles="@Roles.Admin">` existente, después del link `/admin`:
```razor
<MudNavLink Href="/solicitudes-excepcion"
            Icon="@Icons.Material.Filled.PendingActions"
            IconColor="Color.Secondary">
    Solicitudes
</MudNavLink>
```

### Archivos existentes que NO deben tocarse
- `SolicitudExcepcionConfiguration.cs` — la tabla ya tiene las columnas correctas (incluye `Created` por BaseAuditableEntity)
- `ApplicationDbContextInitialiser.cs` — la tabla `SolicitudesExcepcion` ya se crea correctamente
- Tests de Story 3.2 — deben seguir pasando sin cambios

### Ruta correcta de archivos de tests existentes
```
Backend/tests/Domain.UnitTests/          ← tests de dominio (agregar aquí SolicitudExcepcionEstadoTests)
Backend/tests/Application.UnitTests/     ← unit tests validators
Backend/tests/Infrastructure.IntegrationTests/  ← tests de handlers con EF InMemory
```

### Verificar estructura existente de Domain.UnitTests
Antes de crear la carpeta, leer `Backend/tests/Domain.UnitTests/` para ver el patrón existente y seguirlo.

### Estado de tests al iniciar esta story
45 tests pasando (3 Domain + 15 Application + 27 Integration).
Al terminar: mínimo 53 tests (4 nuevos Domain + 4 nuevos Integration + tests existentes sin cambios).

### Verificación de la integración (AC: 2)
Para probar que AC2 funciona end-to-end manualmente:
1. Iniciar sesión como empleado.
2. Intentar guardar un registro para una fecha de hace más de 3 días hábiles → debe fallar.
3. Desde la misma sesión, el sistema habrá enviado la solicitud de excepción (Story 3.2).
4. Iniciar sesión como admin. Ir a /solicitudes-excepcion. Aprobar la solicitud.
5. Volver a iniciar sesión como empleado. Intentar guardar el registro para esa fecha → debe funcionar.

## Dev Agent Record

### Debug Log

_(vacío al inicio)_

### Completion Notes

- Dominio: `SolicitudExcepcion` ahora tiene `Aprobar()` y `Rechazar()` con guarda de estado inmutable.
- Backend CQRS: Query `GetSolicitudesExcepcion` (Admin-only, resuelve email via `IIdentityService`), Commands `AprobarSolicitudExcepcion` y `RechazarSolicitudExcepcion` (Admin-only con `[Authorize(Roles=Roles.Admin)]`).
- Integración clave: `CreateRegistroHorasCommandHandler` verifica excepción aprobada antes de rechazar fechas fuera de ventana.
- Endpoints: GET `/api/solicitudes-excepcion`, POST `/{id}/aprobar`, POST `/{id}/rechazar` agregados al `IEndpointGroup` existente.
- Tests: 54/54 pasando (7D + 15A + 32I). `NotFoundException` usada con alias para resolver ambigüedad con `Ardalis.GuardClauses`.
- Frontend: `SolicitudExcepcionAdminPage` en `/solicitudes-excepcion` (solo Admin), tabla con chips de color por estado, confirmación vía `MudMessageBox`, actualización local sin recarga. Nav link "Solicitudes" visible solo para Admin.

### Implementation Plan

Backend → Frontend. Red-green-refactor por task. Tests de dominio primero para validar la entidad antes de implementar handlers.

## File List

### Nuevos
- `Backend/src/Application/Features/SolicitudesExcepcion/Queries/GetSolicitudesExcepcion/SolicitudExcepcionAdminDto.cs`
- `Backend/src/Application/Features/SolicitudesExcepcion/Queries/GetSolicitudesExcepcion/GetSolicitudesExcepcionQuery.cs`
- `Backend/src/Application/Features/SolicitudesExcepcion/Queries/GetSolicitudesExcepcion/GetSolicitudesExcepcionQueryHandler.cs`
- `Backend/src/Application/Features/SolicitudesExcepcion/Commands/AprobarSolicitudExcepcion/AprobarSolicitudExcepcionCommand.cs`
- `Backend/src/Application/Features/SolicitudesExcepcion/Commands/AprobarSolicitudExcepcion/AprobarSolicitudExcepcionCommandHandler.cs`
- `Backend/src/Application/Features/SolicitudesExcepcion/Commands/RechazarSolicitudExcepcion/RechazarSolicitudExcepcionCommand.cs`
- `Backend/src/Application/Features/SolicitudesExcepcion/Commands/RechazarSolicitudExcepcion/RechazarSolicitudExcepcionCommandHandler.cs`
- `Backend/tests/Domain.UnitTests/SolicitudesExcepcion/SolicitudExcepcionEstadoTests.cs`
- `Backend/tests/Infrastructure.IntegrationTests/SolicitudesExcepcion/AprobarRechazarSolicitudExcepcionTests.cs`
- `Fronted/src/WebUI/Infrastructure/Repositories/Models/SolicitudExcepcionAdminModels.cs`
- `Fronted/src/WebUI/Infrastructure/Repositories/ISolicitudExcepcionAdminRepository.cs`
- `Fronted/src/WebUI/Infrastructure/Repositories/SolicitudExcepcionAdminRepository.cs`
- `Fronted/src/WebUI/Features/Admin/Pages/SolicitudesExcepcionAdminPage.razor`

### Modificados
- `Backend/src/Domain/Entities/SolicitudExcepcion.cs`
- `Backend/src/Api/Endpoints/SolicitudesExcepcion.cs`
- `Backend/src/Application/Features/RegistroHoras/Commands/CreateRegistroHoras/CreateRegistroHorasCommandHandler.cs`
- `Backend/tests/Infrastructure.IntegrationTests/RegistroHoras/CreateRegistroHorasCommandHandlerTests.cs`
- `Fronted/src/WebUI/Layout/NavMenu.razor`
- `Fronted/src/WebUI/Program.cs`

## Change Log

- Creada Story 3.3 (2026-05-14)
- Implementada Story 3.3 completa: dominio + CQRS Admin + excepción aprobada en registro + frontend Admin page (2026-05-14)
