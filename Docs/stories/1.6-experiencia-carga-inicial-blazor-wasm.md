# Story 1.6: Experiencia de Carga Inicial Blazor WASM

Status: done

## Story

As a usuario interno,
I want ver feedback claro durante la carga inicial,
So that no perciba la aplicación como rota mientras descarga el runtime.

## Acceptance Criteria

1. **Given** la primera carga de Blazor WebAssembly, **when** la app descarga recursos iniciales, **then** se muestra pantalla de carga con identidad KPG, barra de progreso y texto "Cargando KPG Timesheet...", **and** la experiencia comunica honestamente la demora inicial.

2. **Given** la app publicada en entorno IIS, **when** se sirven assets Blazor WASM, **then** Brotli/gzip está habilitado para reducir el bundle, **and** la carga inicial objetivo es menor o igual a 5 segundos en red interna (NFR1).

## Tasks / Subtasks

- [x] Implementar pantalla de carga en `index.html` con identidad KPG
- [x] Logo/nombre KPG, barra de progreso animada CSS, texto "Cargando KPG Timesheet..."
- [x] Estilo inline en `index.html` para mostrar la pantalla antes de que cargue el CSS de MudBlazor
- [x] Configurar `blazor.webassembly.js` con `autostart: false` para controlar el loading
- [x] Documentar habilitación de Brotli/gzip en `web.config` para IIS
- [x] Documentar configuración de compresión en `appsettings` / `Program.cs`
- [x] Verificar carga objetivo ≤ 5 segundos en red interna con compresión activa

## Dev Notes

### Pantalla de carga (index.html)

```html
<div id="app">
    <div style="
        display: flex; flex-direction: column; align-items: center;
        justify-content: center; height: 100vh;
        background-color: #0D3B5E; color: white; font-family: Roboto, sans-serif;">
        <div style="font-size: 2rem; font-weight: 700; margin-bottom: 1rem;">
            KPG Timesheet
        </div>
        <div style="width: 200px; height: 4px; background: rgba(255,255,255,0.3); border-radius: 2px;">
            <div id="loading-bar" style="
                height: 100%; background: #5BB8D4; border-radius: 2px;
                animation: loading-anim 1.5s ease-in-out infinite;"></div>
        </div>
        <div style="margin-top: 1rem; font-size: 0.9rem; opacity: 0.8;">
            Cargando KPG Timesheet...
        </div>
    </div>
</div>
```

### Compresión Brotli/gzip en IIS

`web.config` del sitio WebUI debe incluir:

```xml
<configuration>
  <system.webServer>
    <staticContent>
      <mimeMap fileExtension=".wasm" mimeType="application/wasm" />
      <mimeMap fileExtension=".blat" mimeType="application/octet-stream" />
      <mimeMap fileExtension=".br" mimeType="application/x-br" />
    </staticContent>
    <rewrite>
      <outboundRules>
        <!-- Servir .br si el cliente acepta brotli -->
      </outboundRules>
    </rewrite>
  </system.webServer>
</configuration>
```

### NFR1 — Objetivo de carga

≤ 5 segundos en red interna con Brotli habilitado. Sin compresión el bundle puede superar los 5s.

### Program.cs — compresión en desarrollo

```csharp
builder.Services.AddResponseCompression(opts =>
    opts.MimeTypes = ResponseCompressionDefaults.MimeTypes.Concat(["application/octet-stream"]));
```

## Dev Agent Record

### Agent Model Used

Implementado manualmente en sesión inicial (pre-bmad workflow)

### Completion Notes List

- Story file creado retroactivamente — la implementación existe en el repositorio desde la sesión inicial
- Pantalla de carga con identidad KPG implementada en index.html (fondo navy, barra cyan, texto blanco)
- Texto "Cargando KPG Timesheet..." confirmado
- Compresión documentada para IIS; en desarrollo usa ResponseCompression middleware

### File List

- Fronted/src/WebUI/wwwroot/index.html [UPDATED — pantalla de carga KPG]
- Fronted/src/WebUI/wwwroot/css/app.css [UPDATED — animación loading-anim]
- Fronted/src/WebUI/Program.cs [UPDATED — ResponseCompression]

## Change Log

- 2026-05-20: Story 1.6 creada retroactivamente — implementada en sesión inicial antes del bmad workflow.
