# Story 8.1: Web Speech API — Captura de Voz vía JS Interop

Status: done

## Story

As a empleado,
I want que la aplicación pueda escuchar mi voz mediante el micrófono del navegador,
So that el sistema reciba el texto transcrito para procesarlo sin depender de servicios externos.

## Acceptance Criteria

1. **Given** Chrome o Edge con micrófono disponible, **When** el usuario activa el botón de voz, **Then** el navegador solicita permiso de micrófono si aún no fue otorgado **And** comienza a escuchar en español colombiano (es-CO).

2. **Given** el usuario termina de hablar, **When** el reconocimiento de voz finaliza, **Then** el transcript en texto llega al componente Blazor vía callback `[JSInvokable]` **And** el texto se entrega completo (no parcial) para su procesamiento.

3. **Given** Firefox u otro navegador sin soporte de Web Speech API, **When** el usuario intenta activar el micrófono, **Then** se muestra un MudAlert de advertencia indicando que se requiere Chrome o Edge **And** el botón queda deshabilitado.

4. **Given** permiso de micrófono denegado por el usuario, **When** ocurre el error del navegador, **Then** se muestra un mensaje descriptivo orientando al usuario a revisar los permisos del navegador **And** el sistema vuelve al estado idle sin lanzar excepción no controlada.

## Tasks / Subtasks

- [x] **T1 — Crear `voiceInput.js`** (AC: 1, 2, 3, 4)
  - [x] Crear `Fronted/src/WebUI/wwwroot/js/voiceInput.js`
  - [x] Implementar `window.voiceInput.isSupported()` → retorna `true` si `SpeechRecognition` o `webkitSpeechRecognition` está disponible
  - [x] Implementar `window.voiceInput.start(dotNetRef, lang)` con `continuous: false` y auto-restart en `onend`
  - [x] Implementar `window.voiceInput.stop()` → llama `recognition.stop()` si existe instancia activa
  - [x] Guard anti-race-condition en `start()`: si sesión anterior aún activa, esperar `onend` antes de iniciar nueva
  - [x] Manejo silencioso de `no-speech` (reintentar sin contar como error)

- [x] **T2 — Registrar script en index.html** (AC: 1)
  - [x] Agregar `<script src="js/voiceInput.js"></script>` en `Fronted/src/WebUI/wwwroot/index.html`

- [x] **T3 — Crear `VoiceInputService.cs`** (AC: 1, 2, 3, 4)
  - [x] Crear `Fronted/src/WebUI/Shared/Services/VoiceInputService.cs`
  - [x] Métodos: `IsSupportedAsync()`, `InitAsync()`, `StartAsync(dotNetRef, lang)`, `StopAsync()`
  - [x] Registrar como `Scoped` en `Program.cs`

## Dev Notes

- Web Speech API disponible en Chrome 33+ y Edge 79+. Firefox no la soporta.
- `continuous: false` con auto-restart en `onend` es más estable que `continuous: true` en Chrome (evita timeout de ~60s y errores `audio-capture` espontáneos).
- Guard en `start()`: si `recognition !== null`, la sesión anterior aún tiene el mic ocupado a nivel OS. Se captura `onend` antes de llamar `startRecognition()` para evitar `audio-capture`.
- `no-speech` no se cuenta como error — es silencio normal entre frases, se reinicia tras 300ms.
- El `DotNetObjectReference` debe ser dispuesto por el componente que lo crea, no por el servicio.
- No hay cambios al backend en esta story.

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### Completion Notes List
- `voiceInput.js` creado con `continuous: false` + auto-restart en `onend` (patrón más estable que `continuous: true` en Chrome)
- Guard anti-race-condition en `start()`: si `recognition` sigue activo, se captura `onend` antes de arrancar nueva sesión
- `no-speech` manejado silenciosamente (reinicia tras 300ms, no cuenta como error)
- `errorCount` se resetea en resultado exitoso para que errores transitorios no agoten el límite
- `stop()` no nulifica `recognition` — lo hace `onend`, así `start()` detecta mic ocupado
- `VoiceInputService.cs` creado en `Fronted/src/WebUI/Shared/Services/` con los 4 métodos requeridos
- Registrado como Scoped en `Program.cs`
- Script referenciado en `index.html`

### File List
- `Fronted/src/WebUI/wwwroot/js/voiceInput.js` [CREATED]
- `Fronted/src/WebUI/wwwroot/index.html` [MODIFIED]
- `Fronted/src/WebUI/Shared/Services/VoiceInputService.cs` [CREATED]
- `Fronted/src/WebUI/Program.cs` [MODIFIED]

## Change Log
- 2026-06-03: Story 8.1 creada e implementada — Web Speech API con auto-restart y guard anti-race-condition.
