# Story 2.6: Registro Propio para Supervisor

Status: done

## Story

As a supervisor,
I want registrar y eliminar mis propias horas igual que un empleado,
So that mi rol de supervisión no me impida cumplir mi timesheet personal.

## Acceptance Criteria

1. Dado un usuario con rol Supervisor, cuando abre la pantalla de registro personal, entonces puede crear registros propios con los mismos campos que un Empleado, y las reglas de validación son las mismas.
2. Dado un supervisor autenticado, cuando consulta su historial personal, entonces ve sus propios registros, y no se mezclan con registros del equipo en esta vista personal.
3. Dado un supervisor elimina un registro propio, cuando confirma la acción, entonces la eliminación respeta las mismas reglas aplicadas al Empleado, y no requiere funcionalidades futuras de supervisión de equipo.

## Tasks / Subtasks

- [x] Backend — Usuario supervisor en seed (prerequisito para pruebas manuales)
  - [x] En `Backend/src/Infrastructure/Data/ApplicationDbContextInitialiser.cs`, agregar usuario `supervisor@kpg.com` (contraseña `Supervisor1234!`) con rol `Roles.Supervisor`, siguiendo el mismo patrón del usuario `empleado@kpg.com`.

- [x] Backend — Tests de integración con rol Supervisor (AC: 1, 2, 3)
  - [x] Crear `Backend/tests/Infrastructure.IntegrationTests/RegistroHoras/SupervisorRegistroHorasTests.cs` con los siguientes tests:
    - `Handle_WhenSupervisorCreatesRegistro_ShouldPersistWithSupervisorUserId` (AC1)
    - `Handle_WhenSupervisorHasDuplicateRegistro_ShouldThrowValidationException` (AC1 — misma validación)
    - `Handle_WhenSupervisorQueriesHistorial_ShouldReturnOnlyOwnRecords` (AC2)
    - `Handle_WhenSupervisorDeletesOwnRegistro_ShouldRemoveIt` (AC3)
    - `Handle_WhenSupervisorDeletesOtherUserRegistro_ShouldThrowForbiddenAccessException` (AC3 — mismas reglas)

- [x] Build y verificación (AC: todos)
  - [x] `dotnet build Backend/KPG.Timesheet.sln` → 0 errores, 0 warnings.
  - [x] `dotnet test Backend/KPG.Timesheet.sln` → todos pasan (35/35: 3 Domain + 12 Application + 20 Integration).
  - [x] `dotnet build Fronted/KPG.Timesheet.WebUI.sln` → 0 errores, 0 warnings.

## Dev Notes

### Contexto Crítico — Lo que YA Existe

Esta story es **de verificación y habilitación operacional**, NO de nueva funcionalidad. El sistema ya soporta supervisores completamente gracias al diseño role-inclusive de Stories 2.1-2.5:

| Componente | Estado | Detalle |
|---|---|---|
| `RegistroPage.razor` | ✅ YA autoriza Supervisor | `[Authorize(Roles = "Empleado,Supervisor,Gerente,Admin")]` |
| `HistorialPage.razor` | ✅ YA autoriza Supervisor | `[Authorize(Roles = "Empleado,Supervisor,Gerente,Admin")]` |
| `NavMenu.razor` | ✅ YA muestra los links | `AuthorizeView Roles="Empleado,Supervisor,Gerente,Admin"` |
| `POST /api/registros-horas` | ✅ YA autoriza Supervisor | `AuthorizeAttribute` con los 4 roles |
| `GET /api/registros-horas` | ✅ YA autoriza Supervisor | Ídem |
| `DELETE /api/registros-horas/{id}` | ✅ YA autoriza Supervisor | Ídem |
| `CreateRegistroHorasCommandHandler` | ✅ YA funciona | Usa `_user.Id` del JWT — agnostico al rol |
| `GetMisRegistrosQueryHandler` | ✅ YA filtra por userId | `WHERE r.UserId == _user.Id` — no mezcla equipo |
| `DeleteRegistroHorasCommandHandler` | ✅ YA verifica ownership | `if (registro.UserId != _user.Id) throw ForbiddenAccessException` |

**Lo único que falta:**
1. Usuario semilla `supervisor@kpg.com` para testing manual y producción
2. Tests de integración con `TestUser(rol: Supervisor)` que formalmente verifiquen FR9

### Seed User — Implementación Exacta

Agregar en `TrySeedAsync()` de `ApplicationDbContextInitialiser.cs`, **después del bloque de `empleado@kpg.com`**:

```csharp
// Usuario supervisor de prueba
var supervisor = new ApplicationUser { UserName = "supervisor@kpg.com", Email = "supervisor@kpg.com" };
if (_userManager.Users.All(u => u.UserName != supervisor.UserName))
{
    await _userManager.CreateAsync(supervisor, "Supervisor1234!");
    await _userManager.AddToRoleAsync(supervisor, Roles.Supervisor);
}
```

### Tests de Integración — Archivo Completo

**Namespace y usings** (mismos que los otros tests de RegistroHoras):

```csharp
using KPG.Timesheet.Application.Common.Exceptions;
using KPG.Timesheet.Application.Common.Interfaces;
using KPG.Timesheet.Application.Features.RegistroHoras.Commands.CreateRegistroHoras;
using KPG.Timesheet.Application.Features.RegistroHoras.Commands.DeleteRegistroHoras;
using KPG.Timesheet.Application.Features.RegistroHoras.Queries.GetMisRegistros;
using KPG.Timesheet.Domain.Enums;
using KPG.Timesheet.Infrastructure.Data;
using Microsoft.EntityFrameworkCore;
using RegistroHorasEntity = KPG.Timesheet.Domain.Entities.RegistroHoras;

namespace KPG.Timesheet.Infrastructure.IntegrationTests.RegistroHoras;
```

**TestUser debe usar `Roles.Supervisor`** (diferencia clave respecto a tests de Empleado):

```csharp
private sealed class TestUser : IUser
{
    public TestUser(string id) => Id = id;
    public string? Id { get; }
    public List<string>? Roles => [KPG.Timesheet.Domain.Constants.Roles.Supervisor];
}
```

**Tests a implementar:**

```csharp
[Fact]
public async Task Handle_WhenSupervisorCreatesRegistro_ShouldPersistWithSupervisorUserId()
{
    await using var context = CreateContext();
    var handler = new CreateRegistroHorasCommandHandler(context, new TestUser("supervisor-1"));

    var result = await handler.Handle(ValidCommand(), CancellationToken.None);

    result.Id.Should().BeGreaterThan(0);
    result.UserId.Should().Be("supervisor-1");
    context.RegistrosHoras.Should().ContainSingle(r =>
        r.UserId == "supervisor-1" &&
        r.Turno == TurnoRegistro.AM);
}

[Fact]
public async Task Handle_WhenSupervisorHasDuplicateRegistro_ShouldThrowValidationException()
{
    await using var context = CreateContext();
    var handler = new CreateRegistroHorasCommandHandler(context, new TestUser("supervisor-1"));

    await handler.Handle(ValidCommand(), CancellationToken.None);
    var act = () => handler.Handle(ValidCommand(), CancellationToken.None);

    await act.Should().ThrowAsync<ValidationException>();
}

[Fact]
public async Task Handle_WhenSupervisorQueriesHistorial_ShouldReturnOnlyOwnRecords()
{
    await using var context = CreateContext();
    // Supervisor tiene 1 registro, empleado tiene 1 registro
    context.RegistrosHoras.Add(MakeRegistro("supervisor-1", new DateOnly(2026, 5, 10)));
    context.RegistrosHoras.Add(MakeRegistro("empleado-1", new DateOnly(2026, 5, 11)));
    await context.SaveChangesAsync(CancellationToken.None);

    var handler = new GetMisRegistrosQueryHandler(context, new TestUser("supervisor-1"));
    var result = await handler.Handle(new GetMisRegistrosQuery(), CancellationToken.None);

    result.Should().HaveCount(1);
    result.Single().Cliente.Should().Be("KPG");
}

[Fact]
public async Task Handle_WhenSupervisorDeletesOwnRegistro_ShouldRemoveIt()
{
    await using var context = CreateContext();
    var registro = MakeRegistro("supervisor-1", new DateOnly(2026, 5, 10));
    context.RegistrosHoras.Add(registro);
    await context.SaveChangesAsync(CancellationToken.None);

    var handler = new DeleteRegistroHorasCommandHandler(context, new TestUser("supervisor-1"));
    await handler.Handle(new DeleteRegistroHorasCommand(registro.Id), CancellationToken.None);

    context.RegistrosHoras.Should().BeEmpty();
}

[Fact]
public async Task Handle_WhenSupervisorDeletesOtherUserRegistro_ShouldThrowForbiddenAccessException()
{
    await using var context = CreateContext();
    var registro = MakeRegistro("empleado-1", new DateOnly(2026, 5, 10));
    context.RegistrosHoras.Add(registro);
    await context.SaveChangesAsync(CancellationToken.None);

    var handler = new DeleteRegistroHorasCommandHandler(context, new TestUser("supervisor-1"));
    var act = () => handler.Handle(new DeleteRegistroHorasCommand(registro.Id), CancellationToken.None);

    await act.Should().ThrowAsync<ForbiddenAccessException>();
}
```

**Helpers:**

```csharp
private static ApplicationDbContext CreateContext()
{
    var options = new DbContextOptionsBuilder<ApplicationDbContext>()
        .UseInMemoryDatabase(Guid.NewGuid().ToString())
        .Options;
    return new ApplicationDbContext(options);
}

private static RegistroHorasEntity MakeRegistro(string userId, DateOnly fecha,
    TurnoRegistro turno = TurnoRegistro.AM) =>
    new(userId, fecha, turno,
        new TimeOnly(8, 0), new TimeOnly(13, 0),
        "KPG", "Timesheet", "Remoto", "Consultor", "Desarrollo", "Bogota");

private static CreateRegistroHorasCommand ValidCommand() =>
    new(new DateOnly(2026, 5, 14), TurnoRegistro.AM,
        new TimeOnly(8, 0), new TimeOnly(13, 0),
        "KPG", "Timesheet", "Remoto", "Consultor", "Desarrollo", "Bogota");
```

### Conflicto de Naming: ValidationException

Al importar `KPG.Timesheet.Application.Common.Exceptions.ValidationException` y `FluentValidation`, puede haber ambigüedad. Usar alias explícito en los tests si es necesario:

```csharp
using ValidationException = KPG.Timesheet.Application.Common.Exceptions.ValidationException;
```

### Conteo de Tests Post-Story 2.6

| Categoría | Antes | Después |
|---|---|---|
| Domain | 3 | 3 |
| Application | 12 | 12 |
| Integration | 15 | 20 (+5 SupervisorRegistroHorasTests) |
| **Total** | **30** | **35** |

### No Tocar en Frontend

- `RegistroPage.razor`, `HistorialPage.razor`, `NavMenu.razor` — ya funcionan para Supervisor. NO modificar.
- `KpgShiftForm.razor`, repositorios, modelos — no requieren cambios.

### References

- Épicas: `_bmad-output/planning-artifacts/epics.md` — Epic 2, Story 2.6 (FR9, FR19)
- PRD: `_bmad-output/planning-artifacts/prd.md` — FR9: Supervisor puede registrar/eliminar horas propias
- Story 2.1–2.5: `Docs/stories/` — patrones establecidos de handlers, TestUser, CreateContext
- `Backend/src/Infrastructure/Data/ApplicationDbContextInitialiser.cs` — patrón de seed existente
- `Backend/tests/Infrastructure.IntegrationTests/RegistroHoras/CreateRegistroHorasCommandHandlerTests.cs` — patrón TestUser base
- `Backend/src/Api/Endpoints/RegistroHoras.cs` — auth ya role-inclusive para Supervisor

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6

### Debug Log References

Sin issues durante la implementación — story thin con implementación directa.

### Completion Notes List

- Seed user `supervisor@kpg.com` (Supervisor1234!, rol Supervisor) agregado en `TrySeedAsync()` siguiendo el mismo patrón de `empleado@kpg.com`.
- 5 tests de integración creados con `TestUser` que expone `Roles.Supervisor` (a diferencia de los tests de empleado que usan `Roles.Empleado`).
- Los 3 handlers (Create, GetMisRegistros, Delete) funcionan sin cambios — son agnósticos al rol y operan por `_user.Id` del JWT.
- AC2 verificado: `GetMisRegistrosQueryHandler` filtra `WHERE r.UserId == _user.Id` — el supervisor solo ve sus propios registros.
- 35/35 tests pasan: 3 Domain + 12 Application + 20 Integration (+5 nuevos).
- 0 errores, 0 warnings en Backend y Frontend.

### File List

**Backend — modificados:**
- `Backend/src/Infrastructure/Data/ApplicationDbContextInitialiser.cs`

**Backend — nuevos:**
- `Backend/tests/Infrastructure.IntegrationTests/RegistroHoras/SupervisorRegistroHorasTests.cs`

## Change Log

- 2026-05-14: Story 2.6 creada — verificación de registro propio para Supervisor. Implementación thin: seed supervisor@kpg.com + 5 tests de integración con rol Supervisor. UI y backend ya soportan el rol sin cambios adicionales.
- 2026-05-14: Story 2.6 implementada — seed supervisor@kpg.com agregado, 5 integration tests con rol Supervisor creados y pasando. 35/35 tests totales. Estado: review.
