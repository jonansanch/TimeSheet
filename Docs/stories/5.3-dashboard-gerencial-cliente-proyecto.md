# Story 5.3: Dashboard Gerencial por Cliente, Proyecto y Carga

Status: review

## Story

As a gerente,
I want visualizar horas por cliente, proyecto y consultor,
So that pueda gestionar capacidad y priorizar decisiones operativas.

## Acceptance Criteria

1. **Given** un Gerente autenticado, **when** abre el dashboard gerencial, **then** ve distribución de horas por cliente y proyecto **and** ve indicadores de carga de trabajo por consultor.

2. **Given** filtros de período aplicados, **when** el Gerente cambia el rango, **then** las métricas y gráficos se recalculan correctamente **and** las consultas respetan índices para rendimiento.

3. **Given** un usuario sin rol Gerente o Admin, **when** intenta acceder al dashboard gerencial, **then** el API rechaza el acceso **and** la UI no muestra la sección no permitida.

## Tasks / Subtasks

### Backend

- [ ] Crear `Application/Features/Dashboard/Queries/GetDistribucionPorCliente/` — horas agrupadas por Cliente y Proyecto con Dapper
- [ ] Agregar endpoint `GET /api/dashboard/gerencial?desde={}&hasta={}` (Gerente + Admin)

### Frontend

- [ ] Agregar sección `/dashboard` visible solo para Gerente/Admin con:
  - Dos gráficos ApexCharts: horas por Cliente (pie/donut) y horas por Proyecto (bar)
  - KpgStatCard: total horas período, total clientes activos, top cliente

### Verificación

- [ ] Build limpio
- [ ] Verificar visibilidad correcta según rol (Supervisor NO ve sección gerencial)

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### Completion Notes List
- `RegistroHoras` almacena Cliente y Proyecto como strings desnormalizados — no hay JOIN a tablas de catálogo
- Dapper `QueryMultiple` ejecuta las dos queries (por cliente y por proyecto) en un solo round-trip
- Sección gerencial envuelta en `<AuthorizeView Roles="Gerente,Admin">` — Supervisor ve el resto del dashboard pero no esta sección
- Gráficos: donut (horas por cliente) + barra horizontal (horas por proyecto), mismo selector de período que distribución de horas
- `CargarGerencialAsync` se invoca en paralelo con `CargarDistribucionAsync` al cambiar período

### File List
- Backend/src/Application/Features/Dashboard/Queries/GetDashboardGerencial/GetDashboardGerencialQuery.cs
- Backend/src/Application/Features/Dashboard/Queries/GetDashboardGerencial/DashboardGerencialResponse.cs
- Backend/src/Infrastructure/Dashboard/GetDashboardGerencialQueryHandler.cs
- Backend/src/Api/Endpoints/Dashboard.cs
- Fronted/src/WebUI/Infrastructure/Repositories/Models/DashboardModels.cs
- Fronted/src/WebUI/Infrastructure/Repositories/IDashboardRepository.cs
- Fronted/src/WebUI/Infrastructure/Repositories/DashboardRepository.cs
- Fronted/src/WebUI/Features/Dashboard/Pages/DashboardPage.razor

## Change Log
- 2026-05-19: Story 5.3 creada.
