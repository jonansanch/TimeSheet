# Story 3.2: Solicitar Excepción de Registro Fuera de Ventana

Status: done

## Story

As a empleado,
I want solicitar autorización para registrar una fecha fuera de ventana,
So that pueda manejar casos justificados sin salir del sistema.

## Acceptance Criteria

1. Given una fecha fuera de ventana seleccionada, when el usuario activa la solicitud de excepción, then se abre `KpgExceptionDialog` con fecha, explicación y campo de justificación requerido, and el tono visual usa advertencia ámbar, no error destructivo.

2. Given el diálogo de excepción abierto, when la justificación está vacía, then el botón Enviar solicitud permanece deshabilitado, and el foco inicial está en el textarea de justificación.

3. Given una justificación válida, when el usuario envía la solicitud, then se crea una `SolicitudExcepcion` en estado Pendiente en base de datos, and se muestra confirmación inline de solicitud enviada (el diálogo se cierra, aparece alerta de éxito en el formulario).

## Tasks / Subtasks

### Backend

- [x] Crear enum EstadoSolicitud (AC: 3)
  - [x] Crear `Backend/src/Domain/Enums/EstadoSolicitud.cs` con valores `Pendiente`, `Aprobada`, `Rechazada`.

- [x] Crear entidad SolicitudExcepcion (AC: 3)
  - [x] Crear `Backend/src/Domain/Entities/SolicitudExcepcion.cs` heredando `BaseAuditableEntity`.
  - [x] Constructor recibe `string userId`, `DateOnly fechaRegistro`, `string justificacion`; valida que no sean blank; asigna `Estado = EstadoSolicitud.Pendiente`.
  - [x] Propiedades públicas de solo lectura: `UserId`, `FechaRegistro`, `Justificacion`, `Estado`.

- [x] Registrar SolicitudExcepcion en contexto de datos (AC: 3)
  - [x] Agregar `DbSet<SolicitudExcepcion> SolicitudesExcepcion { get; }` en `Backend/src/Application/Common/Interfaces/IApplicationDbContext.cs`.
  - [x] Agregar `public DbSet<SolicitudExcepcion> SolicitudesExcepcion => Set<SolicitudExcepcion>();` en `Backend/src/Infrastructure/Data/ApplicationDbContext.cs`.

- [x] Crear configuración EF para SolicitudExcepcion (AC: 3)
  - [x] Crear `Backend/src/Infrastructure/Data/Configurations/SolicitudExcepcionConfiguration.cs`.
  - [x] Tabla `SolicitudesExcepcion`, `UserId` max 450, `Justificacion` max 1000, `Estado` como `string` max 20, index en `(UserId, FechaRegistro)`.

- [x] Crear tabla SolicitudesExcepcion en ApplicationDbContextInitialiser (AC: 3)
  - [x] En `EnsureTimesheetTablesAsync`, agregar bloque SQL que crea `[dbo].[SolicitudesExcepcion]` si no existe (columnas: `Id INT IDENTITY PK`, `UserId NVARCHAR(450)`, `FechaRegistro DATE`, `Justificacion NVARCHAR(1000)`, `Estado NVARCHAR(20)`, `Created DATETIMEOFFSET`, `CreatedBy NVARCHAR(MAX) NULL`, `LastModified DATETIMEOFFSET`, `LastModifiedBy NVARCHAR(MAX) NULL`).
  - [x] Agregar index en `(UserId, FechaRegistro)`.

- [x] Crear caso de uso CreateSolicitudExcepcion (AC: 3)
  - [x] Crear carpeta `Backend/src/Application/Features/SolicitudesExcepcion/Commands/CreateSolicitudExcepcion/`.
  - [x] Crear `SolicitudExcepcionDto.cs` como `record(int Id, string UserId, DateOnly FechaRegistro, string Justificacion, string Estado)`.
  - [x] Crear `CreateSolicitudExcepcionCommand.cs` como `record : IRequest<SolicitudExcepcionDto>` con propiedades `DateOnly FechaRegistro`, `string Justificacion`.
  - [x] Crear `CreateSolicitudExcepcionCommandValidator.cs` con FluentValidation: `Justificacion` `NotEmpty()` + `MaximumLength(1000)`.
  - [x] Crear `CreateSolicitudExcepcionCommandHandler.cs` con constructor `(IApplicationDbContext, IUser)`. Crea entidad `SolicitudExcepcion`, la agrega al contexto, guarda, retorna DTO mapeado.

- [x] Exponer endpoint POST /api/solicitudes-excepcion (AC: 3)
  - [x] Crear `Backend/src/Api/Endpoints/SolicitudesExcepcion.cs` implementando `IEndpointGroup`.
  - [x] `RoutePrefix = "/api/solicitudes-excepcion"`.
  - [x] `MapPost("/", CreateSolicitudExcepcion).RequireAuthorization()` → retorna `Results.Created($"/api/solicitudes-excepcion/{dto.Id}", dto)`.

- [x] Crear integration tests para CreateSolicitudExcepcion (AC: 3)
  - [x] Crear `Backend/tests/Application.UnitTests/Features/SolicitudesExcepcion/CreateSolicitudExcepcionCommandValidatorTests.cs` con 3 tests de validator (vacío, demasiado largo, válido).
  - [x] Crear `Backend/tests/Infrastructure.IntegrationTests/SolicitudesExcepcion/CreateSolicitudExcepcionCommandHandlerTests.cs` con 3 tests del handler (estado Pendiente, trim justificación, usuario no autenticado).
  - [x] Verificar que `dotnet test Backend/KPG.Timesheet.sln` resulta en 45/45 tests pasando (3 Domain + 15 Application + 27 Integration).

### Frontend

- [x] Crear modelos y repositorio de solicitudes de excepción (AC: 3)
  - [x] Crear `Fronted/src/WebUI/Infrastructure/Repositories/Models/SolicitudExcepcionModels.cs` con `record CreateSolicitudExcepcionRequest(DateOnly FechaRegistro, string Justificacion)` y `record SolicitudExcepcionResponse(int Id, string UserId, DateOnly FechaRegistro, string Justificacion, string Estado)`.
  - [x] Crear `Fronted/src/WebUI/Infrastructure/Repositories/ISolicitudExcepcionRepository.cs` con método `Task<SolicitudExcepcionResponse?> CreateAsync(CreateSolicitudExcepcionRequest request)`.
  - [x] Crear `Fronted/src/WebUI/Infrastructure/Repositories/SolicitudExcepcionRepository.cs` que hace POST a `/api/solicitudes-excepcion` con Bearer token, retorna `SolicitudExcepcionResponse?`, devuelve `null` en error.
  - [x] Registrar en `Fronted/src/WebUI/Program.cs`: `builder.Services.AddScoped<ISolicitudExcepcionRepository, SolicitudExcepcionRepository>()`.

- [x] Crear componente KpgExceptionDialog (AC: 1, 2, 3)
  - [x] Crear `Fronted/src/WebUI/Features/Registro/Components/KpgExceptionDialog.razor`.
  - [x] Usar MudDialog con `TitleContent` de fondo ámbar (`#FFF8E1`), ícono `Icons.Material.Filled.Schedule` Color.Warning, título "Solicitar autorización" y fecha formateada `dd/MM/yyyy`.
  - [x] Body: texto explicativo + `MudTextField<string>` ref `_justificacionField`, Lines=4, MaxLength=1000, Label="Justificación", Variant.Outlined, `@bind-Value="_justificacion"`, `Counter="1000"`.
  - [x] Acciones: `[Cancelar]` Variant.Outlined `OnClick="Cancel"` + `[Enviar solicitud]` Variant.Filled Color.Warning `Disabled="@string.IsNullOrWhiteSpace(_justificacion)"` `OnClick="Submit"`.
  - [x] Parámetros: `[Parameter] public DateOnly Fecha { get; set; }`.
  - [x] Cascading: `[CascadingParameter] IMudDialogInstance MudDialog { get; set; } = default!;`.
  - [x] `Cancel()` llama `MudDialog.Cancel()`. `Submit()` llama `MudDialog.Close(DialogResult.Ok(_justificacion))`.
  - [x] `OnAfterRenderAsync(bool firstRender)`: si `firstRender` → `await _justificacionField.FocusAsync()` (AC: 2).

- [x] Actualizar KpgShiftForm para usar KpgExceptionDialog (AC: 1, 2, 3)
  - [x] Agregar `@inject IDialogService DialogService` y `@inject ISolicitudExcepcionRepository SolicitudExcepcionRepository` en la sección de directivas de `KpgShiftForm.razor`.
  - [x] Agregar campos: `private bool _excepcionEnviada` y `private DateOnly _excepcionFecha`.
  - [x] Reemplazar el método `HandleExceptionRequested(DateOnly fecha)` actual por versión `async Task` que abre `KpgExceptionDialog` vía `DialogService.ShowAsync`, espera result, llama al repositorio si justificacion válida y muestra confirmación.
  - [x] Reemplazar el bloque `@if (_showExceptionInfo)` por `@if (_excepcionEnviada)` con `MudAlert Severity.Success`.
  - [x] En `HandleDateSelected`, resetear `_excepcionEnviada = false`.
  - [x] Remover el campo `private bool _showExceptionInfo`.

- [x] Verificar compilación del frontend (AC: 1, 2, 3)
  - [x] `dotnet build Fronted/KPG.Timesheet.WebUI.sln` → 0 errores, 0 advertencias.

## Dev Notes

### Contexto del proyecto
- Stack: .NET 10 + Blazor WASM + Clean Architecture + CQRS (MediatR) + SQL Server
- UI: MudBlazor v7.x. Tema: Primary=#0D3B5E (navy), Secondary=#5BB8D4 (sky), Warning=#FFD300 (ámbar/naranja KPG)
- Autenticación: JWT Bearer en todos los endpoints de API
- RBAC: Admin, Gerente, Supervisor, Empleado (en `Domain/Constants/Roles.cs`)
- Tests de integración: `Microsoft.EntityFrameworkCore.InMemory`, FluentAssertions, xUnit
- Patrón IEndpointGroup: auto-descubierto por reflexión en `MapEndpoints()`. No se registra manualmente en Program.cs.

### Patrones críticos a seguir

**Entidad con constructor protegido (mismo patrón que RegistroHoras y ParametroSistema):**
```csharp
public class SolicitudExcepcion : BaseAuditableEntity
{
    private SolicitudExcepcion() { } // requerido por EF

    public SolicitudExcepcion(string userId, DateOnly fechaRegistro, string justificacion)
    {
        if (string.IsNullOrWhiteSpace(userId)) throw new ArgumentException("...", nameof(userId));
        if (string.IsNullOrWhiteSpace(justificacion)) throw new ArgumentException("...", nameof(justificacion));
        UserId = userId;
        FechaRegistro = fechaRegistro;
        Justificacion = justificacion.Trim();
        Estado = EstadoSolicitud.Pendiente;
    }

    public string UserId { get; private set; } = string.Empty;
    public DateOnly FechaRegistro { get; private set; }
    public string Justificacion { get; private set; } = string.Empty;
    public EstadoSolicitud Estado { get; private set; }
}
```

**Enum en Domain/Enums (mismo patrón que TurnoRegistro):**
```csharp
namespace KPG.Timesheet.Domain.Enums;
public enum EstadoSolicitud { Pendiente, Aprobada, Rechazada }
```

**Command + DTO (mismo patrón que CreateRegistroHorasCommand):**
```csharp
// Command
public record CreateSolicitudExcepcionCommand(DateOnly FechaRegistro, string Justificacion) : IRequest<SolicitudExcepcionDto>;
// DTO
public record SolicitudExcepcionDto(int Id, string UserId, DateOnly FechaRegistro, string Justificacion, string Estado);
```

**Handler (mismo patrón que CreateRegistroHorasCommandHandler):**
```csharp
public class CreateSolicitudExcepcionCommandHandler : IRequestHandler<CreateSolicitudExcepcionCommand, SolicitudExcepcionDto>
{
    private readonly IApplicationDbContext _context;
    private readonly IUser _user;

    public CreateSolicitudExcepcionCommandHandler(IApplicationDbContext context, IUser user)
    {
        _context = context;
        _user = user;
    }

    public async Task<SolicitudExcepcionDto> Handle(CreateSolicitudExcepcionCommand request, CancellationToken cancellationToken)
    {
        var userId = _user.Id;
        if (string.IsNullOrWhiteSpace(userId))
            throw new UnauthorizedAccessException("No existe usuario autenticado.");

        var solicitud = new SolicitudExcepcion(userId, request.FechaRegistro, request.Justificacion);
        _context.SolicitudesExcepcion.Add(solicitud);
        await _context.SaveChangesAsync(cancellationToken);

        return new SolicitudExcepcionDto(
            solicitud.Id,
            solicitud.UserId,
            solicitud.FechaRegistro,
            solicitud.Justificacion,
            solicitud.Estado.ToString());
    }
}
```

**Validator (mismo patrón que CreateRegistroHorasCommandValidator):**
```csharp
public class CreateSolicitudExcepcionCommandValidator : AbstractValidator<CreateSolicitudExcepcionCommand>
{
    public CreateSolicitudExcepcionCommandValidator()
    {
        RuleFor(x => x.Justificacion).NotEmpty().MaximumLength(1000);
    }
}
```

**Endpoint (mismo patrón que Sistema.cs):**
```csharp
public class SolicitudesExcepcion : IEndpointGroup
{
    public static string RoutePrefix => "/api/solicitudes-excepcion";

    public static void Map(RouteGroupBuilder groupBuilder)
    {
        groupBuilder.MapPost("/", Create).RequireAuthorization();
    }

    [EndpointSummary("Crear solicitud de excepción para registro fuera de ventana")]
    private static async Task<IResult> Create(CreateSolicitudExcepcionCommand command, ISender sender)
    {
        var dto = await sender.Send(command);
        return Results.Created($"/api/solicitudes-excepcion/{dto.Id}", dto);
    }
}
```

**Configuración EF (mismo patrón que RegistroHorasConfiguration):**
```csharp
public class SolicitudExcepcionConfiguration : IEntityTypeConfiguration<SolicitudExcepcion>
{
    public void Configure(EntityTypeBuilder<SolicitudExcepcion> builder)
    {
        builder.ToTable("SolicitudesExcepcion");
        builder.HasKey(s => s.Id);
        builder.Property(s => s.UserId).HasMaxLength(450).IsRequired();
        builder.Property(s => s.FechaRegistro).IsRequired();
        builder.Property(s => s.Justificacion).HasMaxLength(1000).IsRequired();
        builder.Property(s => s.Estado).HasConversion<string>().HasMaxLength(20).IsRequired();
        builder.HasIndex(s => new { s.UserId, s.FechaRegistro });
    }
}
```

**SQL para EnsureTimesheetTablesAsync (agregar DESPUÉS del bloque de ParametrosSistema):**
```sql
IF OBJECT_ID(N'[dbo].[SolicitudesExcepcion]', N'U') IS NULL
BEGIN
    CREATE TABLE [dbo].[SolicitudesExcepcion] (
        [Id] int NOT NULL IDENTITY,
        [UserId] nvarchar(450) NOT NULL,
        [FechaRegistro] date NOT NULL,
        [Justificacion] nvarchar(1000) NOT NULL,
        [Estado] nvarchar(20) NOT NULL,
        [Created] datetimeoffset NOT NULL,
        [CreatedBy] nvarchar(max) NULL,
        [LastModified] datetimeoffset NOT NULL,
        [LastModifiedBy] nvarchar(max) NULL,
        CONSTRAINT [PK_SolicitudesExcepcion] PRIMARY KEY ([Id])
    );
    CREATE INDEX [IX_SolicitudesExcepcion_UserId_FechaRegistro]
        ON [dbo].[SolicitudesExcepcion] ([UserId], [FechaRegistro]);
END
```

**Tests de integración (mismo patrón que VentanaRetroactivoTests):**
```csharp
public class CreateSolicitudExcepcionCommandHandlerTests
{
    [Fact]
    public async Task Handle_WhenValidRequest_ShouldCreateWithEstadoPendiente()
    {
        await using var context = CreateContext();
        var handler = new CreateSolicitudExcepcionCommandHandler(context, new TestUser("user-1"));
        var command = new CreateSolicitudExcepcionCommand(new DateOnly(2026, 4, 1), "Viaje de negocios imprevisto");

        var result = await handler.Handle(command, CancellationToken.None);

        result.Estado.Should().Be("Pendiente");
        result.Id.Should().BeGreaterThan(0);
        context.SolicitudesExcepcion.Should().ContainSingle(s => s.Id == result.Id);
    }

    [Fact]
    public async Task Handle_WhenJustificacionEmpty_ShouldThrowValidationException()
    { ... }

    [Fact]
    public async Task Handle_WhenJustificacionTooLong_ShouldThrowValidationException()
    { ... }

    private static ApplicationDbContext CreateContext() { /* InMemoryDatabase */ }

    private sealed class TestUser : IUser
    {
        public TestUser(string id) => Id = id;
        public string? Id { get; }
        public List<string>? Roles => [Roles.Empleado];
    }
}
```

**IMPORTANTE para tests de validación:** El handler no lanza la `ValidationException` directamente — la lanza el pipeline de validación de MediatR (via `ValidationBehaviour`). En tests de integración del handler puro (sin pipeline), la validación de FluentValidation NO se ejecuta automáticamente. Para testear `ValidationException` por `Justificacion` vacía, hay dos opciones:
- Opción A: Llamar `new CreateSolicitudExcepcionCommandValidator().Validate(command)` manualmente y verificar que falla.
- Opción B: Testear que el handler sí acepta la solicitud si la lógica de negocio lo permite y confiar en el validator independiente.
- **Recomendación:** Crear tests de validator separados (`CreateSolicitudExcepcionCommandValidatorTests`) que prueban las reglas de FluentValidation directamente, similar a los tests en `Application.UnitTests`.

**Verificar si existe Application.UnitTests para los validators:**
```
Backend/tests/Application.UnitTests/
```
Revisar el patrón existente de tests de validator antes de crear nuevos.

### KpgExceptionDialog — Patrón MudDialog

**La forma correcta de abrir el dialog desde KpgShiftForm:**
```csharp
@inject IDialogService DialogService
@inject ISolicitudExcepcionRepository SolicitudExcepcionRepository

// En HandleExceptionRequested (cambiar a async Task):
private async Task HandleExceptionRequested(DateOnly fecha)
{
    var parameters = new DialogParameters<KpgExceptionDialog>
    {
        { x => x.Fecha, fecha }
    };
    var options = new DialogOptions
    {
        MaxWidth = MaxWidth.Small,
        FullWidth = true,
        CloseOnEscapeKey = true
    };
    var dialog = await DialogService.ShowAsync<KpgExceptionDialog>(
        string.Empty, parameters, options);
    var result = await dialog.Result;

    if (result is { Canceled: false, Data: string justificacion }
        && !string.IsNullOrWhiteSpace(justificacion))
    {
        try
        {
            await SolicitudExcepcionRepository.CreateAsync(
                new CreateSolicitudExcepcionRequest(fecha, justificacion));
            _excepcionEnviada = true;
            _excepcionFecha = fecha;
        }
        catch
        {
            _errorMessage = "No fue posible enviar la solicitud. Intente nuevamente.";
        }
    }
}
```

**El título vacío (`string.Empty`) es intencional**: el título se renderiza dentro del `TitleContent` del propio componente para poder aplicar el fondo ámbar.

**KpgExceptionDialog.razor — estructura completa:**
```razor
<MudDialog>
    <TitleContent>
        <div style="background-color: #FFF8E1; margin: -20px -24px 0; padding: 16px 24px; border-radius: 4px 4px 0 0;">
            <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="2">
                <MudIcon Icon="@Icons.Material.Filled.Schedule" Color="Color.Warning" />
                <MudStack Spacing="0">
                    <MudText Typo="Typo.h6" Style="font-weight:600;">Solicitar autorización</MudText>
                    <MudText Typo="Typo.body2" Color="Color.Secondary">@Fecha.ToString("dd/MM/yyyy")</MudText>
                </MudStack>
            </MudStack>
        </div>
    </TitleContent>
    <DialogContent>
        <MudText Typo="Typo.body1" Class="mb-4">
            Esta fecha está fuera de la ventana de registro permitida.
            Describe el motivo para que un administrador pueda aprobarla.
        </MudText>
        <MudTextField @ref="_justificacionField"
                      T="string"
                      Label="Justificación"
                      Variant="Variant.Outlined"
                      Lines="4"
                      @bind-Value="_justificacion"
                      MaxLength="1000"
                      Counter="1000"
                      Required="true"
                      RequiredError="La justificación es requerida."
                      aria-label="Justificación de la solicitud de excepción" />
    </DialogContent>
    <DialogActions>
        <MudButton Variant="Variant.Outlined" OnClick="Cancel">Cancelar</MudButton>
        <MudButton Variant="Variant.Filled"
                   Color="Color.Warning"
                   Disabled="@string.IsNullOrWhiteSpace(_justificacion)"
                   OnClick="Submit">
            Enviar solicitud
        </MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter] IMudDialogInstance MudDialog { get; set; } = default!;
    [Parameter] public DateOnly Fecha { get; set; }

    private MudTextField<string> _justificacionField = default!;
    private string _justificacion = string.Empty;

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
            await _justificacionField.FocusAsync();
    }

    private void Cancel() => MudDialog.Cancel();
    private async Task Submit() => await MudDialog.CloseAsync(_justificacion);
}
```

**Nota UX-DR25 — retorno de foco al cerrar:** MudDialog gestiona el foco automáticamente al cerrarse en MudBlazor 7.x (restores focus to trigger element). No se requiere JS adicional para este comportamiento en la mayoría de navegadores modernos.

### Repositorio frontend (mismo patrón que ParametroSistemaRepository)

```csharp
public class SolicitudExcepcionRepository : ISolicitudExcepcionRepository
{
    private readonly HttpClient _http;
    private readonly IAuthRepository _auth;

    public SolicitudExcepcionRepository(HttpClient http, IAuthRepository auth)
    {
        _http = http;
        _auth = auth;
    }

    public async Task<SolicitudExcepcionResponse?> CreateAsync(CreateSolicitudExcepcionRequest request)
    {
        var token = await _auth.GetAccessTokenAsync();
        if (string.IsNullOrWhiteSpace(token)) return null;

        _http.DefaultRequestHeaders.Authorization =
            new System.Net.Http.Headers.AuthenticationHeaderValue("Bearer", token);

        var response = await _http.PostAsJsonAsync("/api/solicitudes-excepcion", request);
        if (!response.IsSuccessStatusCode) return null;

        return await response.Content.ReadFromJsonAsync<SolicitudExcepcionResponse>();
    }
}
```

### Estado final de KpgShiftForm tras esta story

Los cambios en `KpgShiftForm.razor` son quirúrgicos:
1. Nuevas directivas: `@inject IDialogService DialogService` y `@inject ISolicitudExcepcionRepository SolicitudExcepcionRepository`
2. Remover campo `_showExceptionInfo` → añadir `_excepcionEnviada` y `_excepcionFecha`
3. Cambiar `private void HandleExceptionRequested` → `private async Task HandleExceptionRequested`
4. Actualizar el bloque `@if (_showExceptionInfo)` → `@if (_excepcionEnviada)`
5. En `HandleDateSelected`, resetear `_excepcionEnviada = false`
6. La firma de `HandleExceptionRequested` en `KpgDatePicker.razor` es `EventCallback<DateOnly>` — compatible con `async Task`

### Archivos a no tocar
- `KpgDatePicker.razor`: ya tiene `EventCallback<DateOnly> OnExceptionRequested` correcto
- `KpgSaveConfirmationBanner.razor`: no aplica para esta story (es para registros de horas, no para excepciones)
- Tests existentes (39 tests deben seguir pasando sin cambios)

### IDialogService en Program.cs (frontend)
`IDialogService` es parte de MudBlazor y **ya está registrado** via `builder.Services.AddMudServices()` en `Program.cs`. No requiere registro adicional.

## Dev Agent Record

### Debug Log
- Error CS1061: `IMudDialogInstance` no tiene `CloseAsync` en MudBlazor 8.5.0. Corregido usando `MudDialog.Close(DialogResult.Ok(_justificacion))` sincrónico.

### Completion Notes
- Backend: enum `EstadoSolicitud`, entidad `SolicitudExcepcion` con constructor protegido y estado inmutable `Pendiente`, EF config + tabla SQL con index en (UserId, FechaRegistro), CQRS completo con validator FluentValidation, endpoint `POST /api/solicitudes-excepcion` auto-registrado por reflexión.
- Tests: 3 unit tests de validator + 3 integration tests del handler. Total sube de 39 a 45 (3D+15A+27I).
- Frontend: repositorio `SolicitudExcepcionRepository` con patrón AuthStateService idéntico al ParametroSistemaRepository. `KpgExceptionDialog` usa `MudDialog` con header ámbar #FFF8E1, foco automático en textarea vía `OnAfterRenderAsync(firstRender)`, botón deshabilitado cuando justificacion vacía. `KpgShiftForm` actualizado con cambios quirúrgicos: reemplaza placeholder `_showExceptionInfo` con flujo real, muestra alerta de éxito inline tras envío.

### Implementation Plan
Implementación ejecutada en orden de tasks definidas en story, siguiendo patrón Clean Architecture. Tests escritos antes de marcar tasks completas.

## File List

### Nuevos
- `Backend/src/Domain/Enums/EstadoSolicitud.cs`
- `Backend/src/Domain/Entities/SolicitudExcepcion.cs`
- `Backend/src/Infrastructure/Data/Configurations/SolicitudExcepcionConfiguration.cs`
- `Backend/src/Application/Features/SolicitudesExcepcion/Commands/CreateSolicitudExcepcion/SolicitudExcepcionDto.cs`
- `Backend/src/Application/Features/SolicitudesExcepcion/Commands/CreateSolicitudExcepcion/CreateSolicitudExcepcionCommand.cs`
- `Backend/src/Application/Features/SolicitudesExcepcion/Commands/CreateSolicitudExcepcion/CreateSolicitudExcepcionCommandValidator.cs`
- `Backend/src/Application/Features/SolicitudesExcepcion/Commands/CreateSolicitudExcepcion/CreateSolicitudExcepcionCommandHandler.cs`
- `Backend/src/Api/Endpoints/SolicitudesExcepcion.cs`
- `Backend/tests/Application.UnitTests/Features/SolicitudesExcepcion/CreateSolicitudExcepcionCommandValidatorTests.cs`
- `Backend/tests/Infrastructure.IntegrationTests/SolicitudesExcepcion/CreateSolicitudExcepcionCommandHandlerTests.cs`
- `Fronted/src/WebUI/Infrastructure/Repositories/Models/SolicitudExcepcionModels.cs`
- `Fronted/src/WebUI/Infrastructure/Repositories/ISolicitudExcepcionRepository.cs`
- `Fronted/src/WebUI/Infrastructure/Repositories/SolicitudExcepcionRepository.cs`
- `Fronted/src/WebUI/Features/Registro/Components/KpgExceptionDialog.razor`

### Modificados
- `Backend/src/Application/Common/Interfaces/IApplicationDbContext.cs`
- `Backend/src/Infrastructure/Data/ApplicationDbContext.cs`
- `Backend/src/Infrastructure/Data/ApplicationDbContextInitialiser.cs`
- `Fronted/src/WebUI/Program.cs`
- `Fronted/src/WebUI/Features/Registro/Components/KpgShiftForm.razor`

## Change Log

- Implementada Story 3.2 completa: SolicitudExcepcion backend + KpgExceptionDialog frontend (2026-05-14)
