warning: in the working copy of 'Backend/src/Api/Endpoints/Auth.cs', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'Backend/src/Api/Endpoints/MeResponseDto.cs', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'Backend/src/Infrastructure/Data/ApplicationDbContextInitialiser.cs', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'Backend/tests/Infrastructure.IntegrationTests/Endpoints/AuthEndpointsTests.cs', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'Backend/tests/Infrastructure.IntegrationTests/Endpoints/KpgWebApplicationFactory.cs', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'Fronted/src/WebUI/Features/Auth/Pages/PerfilPage.razor', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'Fronted/src/WebUI/Infrastructure/Repositories/AuthRepository.cs', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'Fronted/src/WebUI/Infrastructure/Repositories/IAuthRepository.cs', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'Fronted/src/WebUI/Infrastructure/Repositories/Models/AuthModels.cs', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'Fronted/src/WebUI/Resources/SharedResource.en.resx', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'Fronted/src/WebUI/Resources/SharedResource.resx', LF will be replaced by CRLF the next time Git touches it
diff --git a/Backend/src/Api/Endpoints/Auth.cs b/Backend/src/Api/Endpoints/Auth.cs
index 3299fbf..16d1ac4 100644
--- a/Backend/src/Api/Endpoints/Auth.cs
+++ b/Backend/src/Api/Endpoints/Auth.cs
@@ -6,7 +6,9 @@ using KPG.Timesheet.Application.Features.Auth.Commands.Login;
 using KPG.Timesheet.Application.Features.Auth.Commands.Logout;
 using KPG.Timesheet.Application.Features.Auth.Commands.Refresh;
 using KPG.Timesheet.Application.Features.Auth.Commands.ResetPassword;
+using KPG.Timesheet.Infrastructure.Identity;
 using MediatR;
+using Microsoft.AspNetCore.Identity;
 using Microsoft.AspNetCore.Mvc;
 
 namespace KPG.Timesheet.Api.Endpoints;
@@ -75,21 +77,33 @@ public class Auth : IEndpointGroup
     }
 
     [EndpointSummary("Usuario actual")]
-    [EndpointDescription("Retorna el userId, email y roles del usuario autenticado.")]
+    [EndpointDescription("Retorna los datos actuales del usuario autenticado desde la base de datos.")]
     [ProducesResponseType<MeResponseDto>(StatusCodes.Status200OK)]
     [ProducesResponseType(StatusCodes.Status401Unauthorized)]
-    public static IResult Me(HttpContext context)
+    public static async Task<IResult> Me(
+        HttpContext context,
+        UserManager<ApplicationUser> userManager,
+        CancellationToken cancellationToken)
     {
-        var user = context.User;
-        var userId = user.FindFirstValue(ClaimTypes.NameIdentifier)
-            ?? user.FindFirstValue(JwtRegisteredClaimNames.Sub)
-            ?? string.Empty;
-        var email = user.FindFirstValue(ClaimTypes.Email)
-            ?? user.FindFirstValue(JwtRegisteredClaimNames.Email)
-            ?? string.Empty;
-        var roles = user.FindAll(ClaimTypes.Role).Select(c => c.Value).ToList();
+        var principal = context.User;
+        var userId = principal.FindFirstValue(ClaimTypes.NameIdentifier)
+            ?? principal.FindFirstValue(JwtRegisteredClaimNames.Sub);
+
+        if (string.IsNullOrWhiteSpace(userId))
+            return Results.Unauthorized();
+
+        var user = await userManager.FindByIdAsync(userId);
+        if (user is null)
+            return Results.Unauthorized();
+
+        var roles = await userManager.GetRolesAsync(user);
 
-        return Results.Ok(new MeResponseDto(userId, email, roles));
+        return Results.Ok(new MeResponseDto(
+            user.Id,
+            user.Email ?? string.Empty,
+            roles.ToList(),
+            user.NombreCompleto,
+            user.CodigoPais));
     }
 
     [EndpointSummary("Cambiar contraseña")]
diff --git a/Backend/src/Api/Endpoints/MeResponseDto.cs b/Backend/src/Api/Endpoints/MeResponseDto.cs
index aebcc69..2eb5a94 100644
--- a/Backend/src/Api/Endpoints/MeResponseDto.cs
+++ b/Backend/src/Api/Endpoints/MeResponseDto.cs
@@ -1,3 +1,8 @@
 namespace KPG.Timesheet.Api.Endpoints;
 
-public record MeResponseDto(string UserId, string Email, IReadOnlyList<string> Roles);
+public record MeResponseDto(
+    string UserId,
+    string Email,
+    IReadOnlyList<string> Roles,
+    string? NombreCompleto,
+    string? CodigoPais);
diff --git a/Backend/src/Infrastructure/Data/ApplicationDbContextInitialiser.cs b/Backend/src/Infrastructure/Data/ApplicationDbContextInitialiser.cs
index 3d45333..7b077bf 100644
--- a/Backend/src/Infrastructure/Data/ApplicationDbContextInitialiser.cs
+++ b/Backend/src/Infrastructure/Data/ApplicationDbContextInitialiser.cs
@@ -1,3 +1,5 @@
+using System.Buffers.Binary;
+using System.Security.Cryptography;
 using System.Text;
 using KPG.Timesheet.Domain.Constants;
 using KPG.Timesheet.Domain.Entities;
@@ -124,7 +126,10 @@ public class ApplicationDbContextInitialiser
         // Tiene un guard adicional porque RunDatabaseInitialiser tambien puede habilitarse
         // fuera de Development para tareas operativas y estas cuentas son solo de QA.
         if (_environment.IsDevelopment())
+        {
             await SeedOrganigramaQaAsync(gerente);
+            await BackfillNacionalidadesDemoAsync();
+        }
 
         // ── Registros de horas (histórico 2 meses) ───────────────────────────
         if (!_context.RegistrosHoras.Any())
@@ -165,6 +170,49 @@ public class ApplicationDbContextInitialiser
         return user;
     }
 
+    private static readonly string[] CodigosPaisDemo =
+    [
+        "AR", "BR", "CA", "CL", "CO", "CR",
+        "EC", "ES", "MX", "PA", "PE", "US"
+    ];
+
+    /// <summary>
+    /// Completa solo datos ausentes en Development. El correo normalizado y SHA-256
+    /// hacen que cada usuario conserve el mismo pais entre reinicios y procesos.
+    /// </summary>
+    private async Task BackfillNacionalidadesDemoAsync()
+    {
+        var usuariosSinPais = await _context.Users
+            .Where(user => user.CodigoPais == null || user.CodigoPais == "")
+            .ToListAsync(CancellationToken.None);
+
+        AsignarNacionalidadesDemo(usuariosSinPais);
+
+        if (usuariosSinPais.Count > 0)
+            await _context.SaveChangesAsync(CancellationToken.None);
+    }
+
+    public static string SeleccionarCodigoPaisDemo(string claveEstable)
+    {
+        ArgumentException.ThrowIfNullOrWhiteSpace(claveEstable);
+        var hash = SHA256.HashData(Encoding.UTF8.GetBytes(claveEstable.Trim().ToUpperInvariant()));
+        var indice = BinaryPrimitives.ReadUInt32BigEndian(hash) % (uint)CodigosPaisDemo.Length;
+        return CodigosPaisDemo[indice];
+    }
+
+    public static int AsignarNacionalidadesDemo(IEnumerable<ApplicationUser> usuarios)
+    {
+        var asignados = 0;
+        foreach (var user in usuarios.Where(user => string.IsNullOrEmpty(user.CodigoPais)))
+        {
+            var claveEstable = user.NormalizedEmail ?? user.Email ?? user.Id;
+            user.CodigoPais = SeleccionarCodigoPaisDemo(claveEstable);
+            asignados++;
+        }
+
+        return asignados;
+    }
+
     private async Task EnsureParametroAsync(string clave, string valor)
     {
         if (!_context.ParametrosSistema.Any(p => p.Clave == clave))
diff --git a/Backend/tests/Infrastructure.IntegrationTests/Endpoints/AuthEndpointsTests.cs b/Backend/tests/Infrastructure.IntegrationTests/Endpoints/AuthEndpointsTests.cs
index 27194ba..544d48b 100644
--- a/Backend/tests/Infrastructure.IntegrationTests/Endpoints/AuthEndpointsTests.cs
+++ b/Backend/tests/Infrastructure.IntegrationTests/Endpoints/AuthEndpointsTests.cs
@@ -99,6 +99,23 @@ public class AuthEndpointsTests : IClassFixture<KpgWebApplicationFactory>, IAsyn
             .Should().Be(KpgWebApplicationFactory.AdminEmail);
         body.RootElement.GetProperty("roles").EnumerateArray()
             .Should().Contain(e => e.GetString() == "Admin");
+        body.RootElement.GetProperty("nombreCompleto").GetString()
+            .Should().Be("Administradora de pruebas");
+        body.RootElement.GetProperty("codigoPais").GetString().Should().Be("CO");
+    }
+
+    [Fact]
+    public async Task Me_WithUserWithoutCountry_ReturnsNullCountry()
+    {
+        var token = await GetTokenAsync(KpgWebApplicationFactory.EmpleadoEmail);
+        using var request = new HttpRequestMessage(HttpMethod.Get, "api/auth/me");
+        request.Headers.Authorization = new System.Net.Http.Headers.AuthenticationHeaderValue("Bearer", token);
+
+        var response = await _client.SendAsync(request);
+
+        response.StatusCode.Should().Be(HttpStatusCode.OK);
+        var body = await ParseJsonAsync(response);
+        body.RootElement.GetProperty("codigoPais").ValueKind.Should().Be(JsonValueKind.Null);
     }
 
     // ── Refresh ────────────────────────────────────────────────────────────
diff --git a/Backend/tests/Infrastructure.IntegrationTests/Endpoints/KpgWebApplicationFactory.cs b/Backend/tests/Infrastructure.IntegrationTests/Endpoints/KpgWebApplicationFactory.cs
index 2a0c484..024d792 100644
--- a/Backend/tests/Infrastructure.IntegrationTests/Endpoints/KpgWebApplicationFactory.cs
+++ b/Backend/tests/Infrastructure.IntegrationTests/Endpoints/KpgWebApplicationFactory.cs
@@ -124,7 +124,15 @@ public class KpgWebApplicationFactory : WebApplicationFactory<Program>
     {
         if (await mgr.FindByEmailAsync(email) is not null) return;
 
-        var user = new ApplicationUser { UserName = email, Email = email, IsActive = true, Created = DateTimeOffset.UtcNow };
+        var user = new ApplicationUser
+        {
+            UserName = email,
+            Email = email,
+            NombreCompleto = role == Roles.Admin ? "Administradora de pruebas" : "Empleado de pruebas",
+            CodigoPais = role == Roles.Admin ? "CO" : null,
+            IsActive = true,
+            Created = DateTimeOffset.UtcNow
+        };
         var result = await mgr.CreateAsync(user, password);
         if (!result.Succeeded)
             throw new InvalidOperationException($"Seed failed for {email}: {string.Join(", ", result.Errors.Select(e => e.Description))}");
diff --git a/Fronted/src/WebUI/Features/Auth/Pages/PerfilPage.razor b/Fronted/src/WebUI/Features/Auth/Pages/PerfilPage.razor
index ad3b24a..185b4ca 100644
--- a/Fronted/src/WebUI/Features/Auth/Pages/PerfilPage.razor
+++ b/Fronted/src/WebUI/Features/Auth/Pages/PerfilPage.razor
@@ -24,10 +24,31 @@
                 <MudDivider Class="mb-4" />
 
                 <MudStack Spacing="3">
+                    @if (!string.IsNullOrWhiteSpace(_nombreCompleto))
+                    {
+                        <div>
+                            <MudText Typo="Typo.caption" Color="Color.Secondary">@L["Profile_LabelName"]</MudText>
+                            <MudText Typo="Typo.body1"><strong>@_nombreCompleto</strong></MudText>
+                        </div>
+                    }
                     <div>
                         <MudText Typo="Typo.caption" Color="Color.Secondary">@L["Profile_LabelEmail"]</MudText>
                         <MudText Typo="Typo.body1"><strong>@_email</strong></MudText>
                     </div>
+                    <div>
+                        <MudText Typo="Typo.caption" Color="Color.Secondary">@L["Profile_LabelNationality"]</MudText>
+                        @if (_pais is not null)
+                        {
+                            <MudText Typo="Typo.body1">
+                                <span role="img" aria-label="@string.Format(L["Profile_NationalityAria"], _pais.Nombre)">@_pais.Bandera</span>
+                                <strong class="ml-1">@_pais.Nombre</strong>
+                            </MudText>
+                        }
+                        else
+                        {
+                            <MudText Typo="Typo.body1" Color="Color.Secondary">@L["Profile_NationalityNotAssigned"]</MudText>
+                        }
+                    </div>
                     <div>
                         <MudText Typo="Typo.caption" Color="Color.Secondary">@L["Profile_LabelRoles"]</MudText>
                         <MudStack Row="true" Spacing="1" Wrap="Wrap.Wrap" Class="mt-1">
@@ -110,6 +131,8 @@
     ];
 
     private string _email = string.Empty;
+    private string? _nombreCompleto;
+    private PaisIso? _pais;
     private List<string> _roles = [];
     private bool _isSaving;
 
@@ -131,6 +154,19 @@
             ?? user.FindFirst("email")?.Value
             ?? string.Empty;
         _roles = user.FindAll(ClaimTypes.Role).Select(c => c.Value).ToList();
+
+        if (!string.IsNullOrWhiteSpace(AuthState.AccessToken))
+        {
+            var perfil = await AuthRepo.GetCurrentUserAsync(AuthState.AccessToken);
+            if (perfil is not null)
+            {
+                _email = perfil.Email;
+                _roles = perfil.Roles;
+                _nombreCompleto = perfil.NombreCompleto;
+                _pais = PaisesIso.Obtener().FirstOrDefault(p =>
+                    string.Equals(p.Codigo, perfil.CodigoPais, StringComparison.OrdinalIgnoreCase));
+            }
+        }
     }
 
     private async Task CambiarPasswordAsync()
diff --git a/Fronted/src/WebUI/Infrastructure/Repositories/AuthRepository.cs b/Fronted/src/WebUI/Infrastructure/Repositories/AuthRepository.cs
index 81605df..da36217 100644
--- a/Fronted/src/WebUI/Infrastructure/Repositories/AuthRepository.cs
+++ b/Fronted/src/WebUI/Infrastructure/Repositories/AuthRepository.cs
@@ -61,6 +61,32 @@ public class AuthRepository : IAuthRepository
         await _http.SendAsync(request, cancellationToken);
     }
 
+    public async Task<CurrentUserResponse?> GetCurrentUserAsync(
+        string accessToken,
+        CancellationToken cancellationToken = default)
+    {
+        using var request = new HttpRequestMessage(HttpMethod.Get, "api/auth/me");
+        request.Headers.Authorization = new AuthenticationHeaderValue("Bearer", accessToken);
+
+        try
+        {
+            using var response = await _http.SendAsync(request, cancellationToken);
+            if (!response.IsSuccessStatusCode)
+                return null;
+
+            return await response.Content.ReadFromJsonAsync<CurrentUserResponse>(
+                cancellationToken: cancellationToken);
+        }
+        catch (HttpRequestException)
+        {
+            return null;
+        }
+        catch (TaskCanceledException) when (!cancellationToken.IsCancellationRequested)
+        {
+            return null;
+        }
+    }
+
     public async Task<(bool Ok, string? Error)> ChangePasswordAsync(string currentPassword, string newPassword, string accessToken, CancellationToken cancellationToken = default)
     {
         using var request = new HttpRequestMessage(HttpMethod.Post, "api/auth/change-password");
diff --git a/Fronted/src/WebUI/Infrastructure/Repositories/IAuthRepository.cs b/Fronted/src/WebUI/Infrastructure/Repositories/IAuthRepository.cs
index 6a99200..9280040 100644
--- a/Fronted/src/WebUI/Infrastructure/Repositories/IAuthRepository.cs
+++ b/Fronted/src/WebUI/Infrastructure/Repositories/IAuthRepository.cs
@@ -7,6 +7,7 @@ public interface IAuthRepository
     Task<LoginResponse?> LoginAsync(LoginRequest request, CancellationToken cancellationToken = default);
     Task<RefreshResponse?> RefreshAsync(string refreshToken, CancellationToken cancellationToken = default);
     Task LogoutAsync(string refreshToken, string accessToken, CancellationToken cancellationToken = default);
+    Task<CurrentUserResponse?> GetCurrentUserAsync(string accessToken, CancellationToken cancellationToken = default);
     Task<(bool Ok, string? Error)> ChangePasswordAsync(string currentPassword, string newPassword, string accessToken, CancellationToken cancellationToken = default);
     Task<bool> ForgotPasswordAsync(string email, CancellationToken cancellationToken = default);
     Task<(bool Ok, string? Error)> ResetPasswordAsync(string email, string token, string newPassword, CancellationToken cancellationToken = default);
diff --git a/Fronted/src/WebUI/Infrastructure/Repositories/Models/AuthModels.cs b/Fronted/src/WebUI/Infrastructure/Repositories/Models/AuthModels.cs
index 367f2ab..f81236d 100644
--- a/Fronted/src/WebUI/Infrastructure/Repositories/Models/AuthModels.cs
+++ b/Fronted/src/WebUI/Infrastructure/Repositories/Models/AuthModels.cs
@@ -21,6 +21,13 @@ public record RefreshResponse(
 
 public record LogoutRequest(string RefreshToken);
 
+public record CurrentUserResponse(
+    string UserId,
+    string Email,
+    List<string> Roles,
+    string? NombreCompleto,
+    string? CodigoPais);
+
 public record ChangePasswordRequest(string CurrentPassword, string NewPassword);
 
 public record ForgotPasswordRequest(string Email);
diff --git a/Fronted/src/WebUI/Resources/SharedResource.en.resx b/Fronted/src/WebUI/Resources/SharedResource.en.resx
index 3b60b9b..bf636d2 100644
--- a/Fronted/src/WebUI/Resources/SharedResource.en.resx
+++ b/Fronted/src/WebUI/Resources/SharedResource.en.resx
@@ -710,6 +710,10 @@
   <data name="Profile_SectionPassword"><value>Change password</value></data>
   <data name="Profile_LabelEmail"><value>Email address</value></data>
   <data name="Profile_LabelRoles"><value>Assigned roles</value></data>
+  <data name="Profile_LabelName"><value>Full name</value></data>
+  <data name="Profile_LabelNationality"><value>Nationality</value></data>
+  <data name="Profile_NationalityAria"><value>Nationality: {0}</value></data>
+  <data name="Profile_NationalityNotAssigned"><value>Not assigned</value></data>
   <data name="Profile_CurrentPassword"><value>Current password</value></data>
   <data name="Profile_NewPassword"><value>New password</value></data>
   <data name="Profile_ConfirmPassword"><value>Confirm new password</value></data>
diff --git a/Fronted/src/WebUI/Resources/SharedResource.resx b/Fronted/src/WebUI/Resources/SharedResource.resx
index 4146370..79089a8 100644
--- a/Fronted/src/WebUI/Resources/SharedResource.resx
+++ b/Fronted/src/WebUI/Resources/SharedResource.resx
@@ -710,6 +710,10 @@
   <data name="Profile_SectionPassword"><value>Cambiar contraseña</value></data>
   <data name="Profile_LabelEmail"><value>Correo electrónico</value></data>
   <data name="Profile_LabelRoles"><value>Roles asignados</value></data>
+  <data name="Profile_LabelName"><value>Nombre completo</value></data>
+  <data name="Profile_LabelNationality"><value>Nacionalidad</value></data>
+  <data name="Profile_NationalityAria"><value>Nacionalidad: {0}</value></data>
+  <data name="Profile_NationalityNotAssigned"><value>No asignada</value></data>
   <data name="Profile_CurrentPassword"><value>Contraseña actual</value></data>
   <data name="Profile_NewPassword"><value>Nueva contraseña</value></data>
   <data name="Profile_ConfirmPassword"><value>Confirmar nueva contraseña</value></data>


diff --git a/Backend/tests/Infrastructure.IntegrationTests/Data/NacionalidadDemoTests.cs b/Backend/tests/Infrastructure.IntegrationTests/Data/NacionalidadDemoTests.cs
new file mode 100644
--- /dev/null
+++ b/Backend/tests/Infrastructure.IntegrationTests/Data/NacionalidadDemoTests.cs
+using KPG.Timesheet.Application.Common.Models;
+using KPG.Timesheet.Infrastructure.Data;
+using KPG.Timesheet.Infrastructure.Identity;
+
+namespace KPG.Timesheet.Infrastructure.IntegrationTests.Data;
+
+public class NacionalidadDemoTests
+{
+    [Fact]
+    public void SeleccionarCodigoPaisDemo_IsStableAndReturnsValidIsoCode()
+    {
+        var primero = ApplicationDbContextInitialiser.SeleccionarCodigoPaisDemo("persona@kpg.com");
+        var segundo = ApplicationDbContextInitialiser.SeleccionarCodigoPaisDemo(" PERSONA@KPG.COM ");
+
+        segundo.Should().Be(primero);
+        CodigoPaisIso.EsValido(primero).Should().BeTrue();
+    }
+
+    [Fact]
+    public void SeleccionarCodigoPaisDemo_ProducesVariedCountries()
+    {
+        var codigos = Enumerable.Range(1, 40)
+            .Select(numero => ApplicationDbContextInitialiser.SeleccionarCodigoPaisDemo($"persona{numero}@kpg.com"))
+            .Distinct()
+            .ToList();
+
+        codigos.Should().HaveCountGreaterThan(1);
+    }
+
+    [Fact]
+    public void AsignarNacionalidadesDemo_PreservesExistingCountryAndIsIdempotent()
+    {
+        var existente = new ApplicationUser { Id = "1", Email = "existente@kpg.com", CodigoPais = "CO" };
+        var pendiente = new ApplicationUser { Id = "2", Email = "pendiente@kpg.com" };
+        var usuarios = new[] { existente, pendiente };
+
+        ApplicationDbContextInitialiser.AsignarNacionalidadesDemo(usuarios).Should().Be(1);
+        var asignado = pendiente.CodigoPais;
+        ApplicationDbContextInitialiser.AsignarNacionalidadesDemo(usuarios).Should().Be(0);
+
+        existente.CodigoPais.Should().Be("CO");
+        pendiente.CodigoPais.Should().Be(asignado);
+        CodigoPaisIso.EsValido(asignado).Should().BeTrue();
+    }
+}
+
+
