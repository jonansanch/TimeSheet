@using KPG.Timesheet.WebUI.Infrastructure.Repositories.Models

<MudPaper Class="pa-0" Elevation="2" Style="border-radius:12px;overflow:hidden;">
    <MudStack Spacing="0">
        <MudStack Class="pa-3" Row="true" AlignItems="AlignItems.Center" Justify="Justify.SpaceBetween"
                  Style="background:var(--mud-palette-surface);">
            <MudText Typo="Typo.subtitle1" Style="font-weight:600;">@Nombre</MudText>
            <MudChip T="string"
                     Size="Size.Small"
                     Color="@EstadoColor"
                     Variant="Variant.Filled">
                @Estado
            </MudChip>
        </MudStack>
        <MudDivider />
        <MudStack Class="pa-3" Row="true" Spacing="4">
            <MudStack Spacing="0" AlignItems="AlignItems.Center">
                <MudText Typo="Typo.subtitle2" Style="font-weight:600;">@HorasFormateadas</MudText>
                <MudText Typo="Typo.caption" Color="Color.Secondary">Horas</MudText>
            </MudStack>
            <MudStack Spacing="0" AlignItems="AlignItems.Center">
                <MudText Typo="Typo.subtitle2" Style="font-weight:600;">@HorariosRegistrados</MudText>
                <MudText Typo="Typo.caption" Color="Color.Secondary">Horarios</MudText>
            </MudStack>
        </MudStack>
    </MudStack>
</MudPaper>

@code {
    [Parameter, EditorRequired] public string Nombre { get; set; } = default!;
    [Parameter, EditorRequired] public string Estado { get; set; } = default!;
    [Parameter] public int TotalMinutos { get; set; }
    [Parameter] public int HorariosRegistrados { get; set; }

    private string HorasFormateadas => $"{TotalMinutos / 60}:{TotalMinutos % 60:00}";

    private Color EstadoColor => Estado switch
    {
        "Completo"  => Color.Success,
        "Parcial"   => Color.Warning,
        _           => Color.Default
    };
}
