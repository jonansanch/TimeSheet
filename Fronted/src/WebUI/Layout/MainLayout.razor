@inherits LayoutComponentBase
@inject KpgAuthStateProvider AuthProvider
@inject NavigationManager Navigation

<MudLayout>
    <MudAppBar Elevation="1" Color="Color.Primary" Dense="false" Style="height:64px;">
        <MudText Typo="Typo.h6" Style="font-family:'Poppins',sans-serif; font-weight:600; letter-spacing:0.5px;">
            KPG Timesheet
        </MudText>
        <MudSpacer />
        <AuthorizeView>
            <Authorized>
                <MudText Typo="Typo.body2" Class="me-3 d-none d-sm-block" Style="opacity:0.85;">
                    @(context.User.FindFirst("email")?.Value ?? context.User.FindFirst(System.Security.Claims.ClaimTypes.Email)?.Value ?? context.User.Identity?.Name)
                </MudText>
                <MudIconButton Icon="@Icons.Material.Filled.Logout"
                               Color="Color.Inherit"
                               title="Cerrar sesión"
                               OnClick="HandleLogout" />
            </Authorized>
        </AuthorizeView>
    </MudAppBar>

    <MudDrawer Open="true" Variant="DrawerVariant.Persistent" Elevation="0"
               Style="background-color:#0D3B5E; border-right:none;">
        <MudDrawerHeader Style="height:64px; display:flex; align-items:center; padding:0 16px;">
            <MudIcon Icon="@Icons.Material.Filled.AccessTime" Style="color:#5BB8D4; margin-right:8px;" />
            <MudText Typo="Typo.subtitle1" Style="color:white; font-family:'Poppins',sans-serif; font-weight:600;">
                Menú
            </MudText>
        </MudDrawerHeader>
        <NavMenu />
    </MudDrawer>

    <MudMainContent>
        <MudContainer MaxWidth="MaxWidth.False" Class="py-6 px-6" Style="max-width:1200px;">
            @Body
        </MudContainer>
    </MudMainContent>
</MudLayout>

@code {
    private async Task HandleLogout()
    {
        Navigation.NavigateTo("/login", replace: true);
        await AuthProvider.LogoutAsync();
    }
}
