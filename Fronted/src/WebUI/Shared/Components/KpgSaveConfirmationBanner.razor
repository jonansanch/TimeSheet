@implements IDisposable

@if (IsVisible)
{
    <MudPaper Elevation="2" Class="pa-3 mb-2" role="status" aria-live="polite">
        <MudStack Row="true" AlignItems="AlignItems.Center" Justify="Justify.SpaceBetween">
            <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="2">
                <MudIcon Icon="@Icons.Material.Filled.CheckCircle" Color="Color.Success" />
                <div>
                    <MudText Typo="Typo.body1">
                        Jornada guardada — @Fecha.ToString("dd/MM/yyyy")
                    </MudText>
                    <MudText Typo="Typo.body2" Color="Color.Secondary">
                        @if (HoraEntradaAM.HasValue && HoraSalidaAM.HasValue)
                        {
                            <span>AM: @HoraEntradaAM.Value.ToString(@"HH\:mm")–@HoraSalidaAM.Value.ToString(@"HH\:mm")</span>
                        }
                        @if (HoraEntradaPM.HasValue && HoraSalidaPM.HasValue)
                        {
                            <span class="ml-2">PM: @HoraEntradaPM.Value.ToString(@"HH\:mm")–@HoraSalidaPM.Value.ToString(@"HH\:mm")</span>
                        }
                        | @Cliente | @Proyecto
                    </MudText>
                </div>
            </MudStack>
            <MudIconButton Icon="@Icons.Material.Filled.Close"
                           Size="Size.Small"
                           aria-label="Cerrar confirmación"
                           OnClick="Dismiss" />
        </MudStack>
    </MudPaper>
}

@code {
    [Parameter] public bool IsVisible { get; set; }
    [Parameter] public EventCallback OnDismiss { get; set; }
    [Parameter] public DateOnly Fecha { get; set; }
    [Parameter] public TimeOnly? HoraEntradaAM { get; set; }
    [Parameter] public TimeOnly? HoraSalidaAM  { get; set; }
    [Parameter] public TimeOnly? HoraEntradaPM { get; set; }
    [Parameter] public TimeOnly? HoraSalidaPM  { get; set; }
    [Parameter] public string Cliente  { get; set; } = string.Empty;
    [Parameter] public string Proyecto { get; set; } = string.Empty;

    private CancellationTokenSource? _cts;

    protected override void OnParametersSet()
    {
        if (IsVisible)
        {
            _cts?.Cancel();
            _cts = new CancellationTokenSource();
            _ = AutoDismissAsync(_cts.Token);
        }
    }

    private async Task AutoDismissAsync(CancellationToken token)
    {
        try
        {
            await Task.Delay(8000, token);
            await InvokeAsync(async () =>
            {
                await OnDismiss.InvokeAsync();
            });
        }
        catch (OperationCanceledException) { }
    }

    private Task Dismiss() => OnDismiss.InvokeAsync();

    public void Dispose() => _cts?.Cancel();
}
