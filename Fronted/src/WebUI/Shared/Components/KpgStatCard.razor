<KpgCard Compact="true" Class="kpg-stat-card">
    <div class="kpg-stat-card__header">
        <span class="kpg-stat-card__icon"><MudIcon Icon="@Icon" Color="@Color" Size="Size.Medium" /></span>
        <MudText Typo="Typo.body2" Class="kpg-stat-card__label">@Label</MudText>
    </div>
    <MudText Typo="Typo.h4" Class="kpg-stat-card__value">@Value</MudText>
</KpgCard>

@code {
    [Parameter, EditorRequired] public string Label { get; set; } = default!;
    [Parameter, EditorRequired] public int Value { get; set; }
    [Parameter, EditorRequired] public string Icon { get; set; } = default!;
    [Parameter] public Color Color { get; set; } = Color.Default;
}
